# Reverification Subagent 06 — App Platform Lifecycle

**Focus:** App Platform Lifecycle — app create/deploy/start/stop/restart/update/rollback/delete/scale/health/version
**Scope:** Reconcile `phase-01/subagent-01-lifecycle.md` 18 capabilities C01-C18 vs `FINAL_PARITY_AUDIT.md §3 AP-01..AP-18` (documented as 20 rows, actual 18 rows `AP-01..AP-18`) vs `final-parity/subagent-02-app-platform.md` 20 rows (01-20)
**Date:** 2026-08-24 (reverification post-Phase-06 checkout)
**Mode:** Read-only. All claims `file:line` verified on current `forge` checkout. No product code modified.
**References inspected:** `reference/app-platforms/{coolify,dokku,caprover,komodo,portainer,1panel,uncloud,docker-compose}` + `forge/api/{internal/services/{apphosting,deployment,compose,replicamanager},internal/http/handlers_*,internal/store/*,internal/daemon/*}` + `beacon/internal/server/compose.go` + `forge/web/lib/api/{apps,compose,deployments}.ts`

---

## 1. Methodology & Corpus Re-inspected

**Reference lifecycle truth inspected (file:line):**

| Ref | Path | Symbol |
|-----|------|--------|
| Coolify | `reference/app-platforms/coolify/app/Models/Application.php:118` `class Application extends BaseModel` | unified workload identity `health_check_*` `status:Attribute 830-876` `compose_parsing_version:192` `isRunning:792` |
| Coolify | `reference/app-platforms/coolify/app/Jobs/ApplicationDeploymentJob.php:42` `class ApplicationDeploymentJob` `handle:283` `rolling_update:1904` `handleStatusTransition:4772` | queue-backed deploy with pre/post hooks |
| Dokku | `reference/app-platforms/dokku/plugins/ps/ps.go:61` `Formation` `Rebuild:78` `Restart:91` `RestartProcess` + `plugins/ps/subcommands.go` `ps:report|restart|start|stop|scale|restore` | plugin-composed lifecycle, `DOKKU_SCALE` file truth |
| Dokku | `reference/app-platforms/dokku/plugins/apps/apps.go:5` `CreateApp` fs truth, `plugins/checks/report.go:7` health gate | imperative app create/destroy |
| CapRover | `reference/app-platforms/caprover/src/models/ICaptainDefinition.ts:1` `ICaptainDefinition{schemaVersion,dockerfileLines,imageName,templateId}` + `src/user/ImageMaker.ts:46/145/434` `convertCaptainDefinitionToDockerfile` | single-file app contract |
| Komodo | `reference/app-platforms/komodo/bin/core/src/api/write/stack.rs` `CreateStack|UpdateStack|DeployStack|StartStack|StopStack|RemoveStack` + `bin/periphery/src/api/compose.rs:344` `ComposeUp` per-stack `sync.RWMutex` | Core→Periphery gRPC `compose up -d`, monitor `monitor/mod.rs:48` poll |
| Portainer | `reference/app-platforms/portainer/api/stacks/stackbuilders/{compose_file_builder.go:22,director.go}` `CreateStackBuilder→Deploy` + `api/stacks/deployments/deployer.go:1` `StackDeployer.Deploy` | builder pattern + Swarm/compose dispatch |
| 1Panel | `reference/app-platforms/1panel/agent/app/model/app_install.go:11` `AppInstall{Name,AppId,Version,Status running\|stopped\|error,DockerCompose,ContainerName}` + `agent/app/service/app_install.go:246` `Operate{start,stop,restart,rebuild,upgrade,delete}` → `docker compose {up,down,restart}` + `SyncAppInstallStatus:835` poll | FS+DB dual truth with reconciler |
| Uncloud | `reference/app-platforms/uncloud/pkg/client/service.go:24` `RunService → Validate→Inspect→VolumeScheduler→Deployment.Run` + `StopService:165` `StartService:199` `RemoveService:141` fan-out `wg.Go` per `MachineMember_UP` | mesh imperative, generation-gated idempotency |
| Compose spec | `reference/app-platforms/docker-compose/cmd/compose/compose.go:561` `restartCommand` `create.go:51 scale[]` `dependencies.go:78` `InDependencyOrder` | canonical `up|down|restart|scale --scale SERVICE=NUM` |

**Forge surfaces re-inspected (file:line):**

| Layer | File:Line | Symbol |
|-------|-----------|--------|
| AppHosting service | `forge/api/internal/services/apphosting/service.go:92` `CreateApp` `service.go:139` `UpdateApp` `service.go:175` `DeleteApp` `service.go:398` `ScaleService` `service.go:681` `TriggerDeploy` `service.go:734` `resolveDeployImage` | 3 source types `GIT\|DOCKER_IMAGE\|COMPOSE:81` |
| Deployment service | `forge/api/internal/services/deployment/service.go:268` `StartBlueGreen` `service.go:276` `rollout rec/roll/canary` | 4 strategies, `isActiveStatus` guard |
| Deployment execution | `forge/api/internal/services/deployment/execution.go:22` `ExecuteDeployment` `execution.go:159` `executeStep` `execution.go:249` `RuntimeExecutor` `execution.go:264` `executeProvisionStep` `execution.go:277` `executePromoteStep` `execution.go:299` `drain/scale` `execution.go:338` `verifyObservedRunning` `execution.go:389` `ResumeDeployments` | lease-claimed DAG, honest-failure gate |
| Deployment rollout | `forge/api/internal/services/deployment/rollout.go:42` `StartRollout` `rollout.go:71` `validateHealthGateTarget` `rollout.go:92` `applyRolloutRequest` `rollout.go:322` `validateImageRef@sha256` | digest enforcement, targetReplicas default |
| Health gate | `forge/api/internal/services/deployment/healthgate.go:11` `CheckHealth` `healthgate.go:62` `resolveNodeHost` `healthgate.go:79` `WaitForHealthGate` | node-derived host, 10s HTTP client |
| Revisions | `forge/api/internal/services/deployment/revisions.go:58` `configHash` `revisions.go:82` `CreateRevision` `revisions.go:147` `RollbackToRevision` `revisions.go:202` `RollbackToPrevious` `revisions.go:225` `CompareRevisions` | per-deployment revision numbers |
| Store apphosting | `forge/api/internal/store/store_apphosting.go:11` `Application` `store_apphosting.go:151` `CreateApplication` `store_apphosting.go:211` `ListApplications` `store_apphosting.go:374` `DeleteApplication` `store_apphosting.go:379` `UpdateApplicationStatus` | `desired_state running\|stopped\|removed` `observed_status idle\|running\|done\|error\|deploying` |
| Store deployments | `forge/api/internal/store/store_deployments.go:14` `Deployment` `store_deployments.go:48` `CreateDeployment` `store_deployments.go:128` `UpdateDeployment` (non-versioned) `store_deployments.go:195` `UpdateDeploymentConfig` versioned `store_deployments.go:300` `ClaimExecutionLease` `store_deployments.go:342` `ReleaseExecutionLeaseIfOwner` vs `353` `ReleaseExecutionLease` unconditional | lease + version fencing |
| Replicamanager | `forge/api/internal/services/replicamanager/service.go:100` `CreateApp` `service.go:143` `DeployApp` `service.go:361` `ScaleApp` `service.go:501` `DeleteApp` `service.go:732` `reconcile` | generation fencing, `BeaconCommandLog`, 64-shard `appLocks`, 1m `reconcile` poll |
| Compose lifecycle | `forge/api/internal/services/compose/lifecycle.go:188` `WaitForHealthy` `lifecycle.go:249` `DeployComposeStack` `lifecycle.go:476` `UpdateComposeStack` `lifecycle.go:570` `DeleteComposeStack` `lifecycle.go:697` `StartStack` `lifecycle.go:745` `StopStack` `lifecycle.go:781` `RestartStack` `lifecycle.go:844` `rollbackStack` `lifecycle.go:988` `computeHash` | scheduler placement, reservation, 2m poll restart-count guard |
| Compose parser | `forge/api/internal/services/compose/service.go:240` `ValidateCompose` `service.go:352` `interpolateEnv` `service.go:594` `checkVolumesSecurity` | 1 MB cap, security deny-list |
| Handlers apphosting | `forge/api/internal/http/handlers_apphosting.go:173` `POST /apps` `handlers_apphosting.go:450` `POST /apps/:id/start` `handlers_apphosting.go:475` `stop` `handlers_apphosting.go:500` `restart` via `SendPower` `handlers_apphosting.go:444` `POST .../instances/:id/{start,stop,restart}` `handlers_apphosting.go:416` `PATCH .../services/:serviceId` scale sync | tenantAccess vs global `GET /apps:138` |
| Handlers deployment | `forge/api/internal/http/handlers_deployment.go:24` `/blue-green` `handlers_deployment.go:44` `rollback` `handlers_deployment.go:68` `execute` | admin-scoped + `adminIPAccess` |
| Handlers revisions | `forge/api/internal/http/handlers_revisions.go:16` `GET /:id/revisions` `handlers_revisions.go:32` `POST /:id/revisions/:revId/rollback` `handlers_revisions.go:48` `POST /:id/rollout` `handlers_revisions.go:64` `compare` | |
| Handlers compose | `forge/api/internal/http/handlers_compose.go:83` `POST /compose/webhook/:webhookId` HMAC `handlers_compose.go:106` `validate` `handlers_compose.go:413` `POST /compose/:id/deploy` now `UpdateComposeStack` `handlers_compose.go:442` `stop` `handlers_compose.go:454` `start` `handlers_compose.go:470` `restart` NEW `handlers_compose.go:483` `logs/status` | admin + user duality |
| Handlers user console | `forge/api/internal/http/handlers_user_console.go:231` `POST /user/compose/:id/restart` → `RestartStack` | user-owned, ownership-guarded `loadUserOwnedStack:301` |
| Beacon compose | `beacon/internal/server/compose.go:77` `validateComposePolicy` `compose.go:344` `handleComposeDeploy` `compose.go:418` `handleComposeStop` `compose.go:462` `handleComposeStart` `compose.go:506` `handleComposeRestart` `compose.go:550` `handleComposeDelete` `compose.go:604` `handleComposeStatus` `compose.go:661` `handleComposeLogs` | per-stack `RWMutex:39` `validStackID:59` `encodeComposeEnv:264` shellout `docker compose` |
| Web apps | `forge/web/lib/api/apps.ts:255` `startApp` `apps.ts:259` `stopApp` `apps.ts:263` `restartApp` `apps.ts:232` `fetchApps` `apps.ts:275` `triggerDeploy` | no `restartComposeStack` |
| Web compose | `forge/web/lib/api/compose.ts:34` `validateCompose` `compose.ts:49` `createComposeStack` `compose.ts:88` `deployComposeStack` `compose.ts:93` `stopComposeStack` `compose.ts:98` `startComposeStack` **NO** `restartComposeStack` exported | gap vs backend |
| Web deployments | `forge/web/lib/api/deployments.ts:88` `fetchDeploymentSteps` `deployments.ts:98` `fetchDeployment` `deployments.ts:103` `fetchDeploymentRevisions` `deployments.ts:113` `rollbackToRevision` | |

---

## 2. Reconciliation: Phase-01 C01-C18 vs FINAL_PARITY_AUDIT AP-01..AP-18 vs final-parity Subagent-02 20 rows

### 2.1 Capability Mapping Ledger

| Phase-01 Row | Phase-01 Title | Phase-01 Status | FINAL_PARITY_AUDIT §3 Row | Final-Parity Title | Final-Parity Status | Subagent-02 Row | Subagent-02 Status | Reconciled Current Verdict |
|---|---|---|---|---|---|---|---|---|
| **C01** | Application Create | `PARTIAL` server_id nullable, composeContent no eagerly validate | **AP-01** `App create (source types image/git/compose)` | `COMPLETE` — handlers_apphosting.go:173 + store_apphosting.go:11 | **01** `App create source types` | `PARTIAL` (3 types only vs Coolify 5 builders, no Tar) | **Current: PARTIAL (honest)** — `apphosting/service.go:81` 3 types validated case-insensitive; `CreateApp:100` defaults to `DOCKER_IMAGE` silently when empty (mask error) vs CapRover exactly-one rule `ImageMaker.ts:416` still missing; `server_id` still nullable at create (deferred to `TriggerDeploy:695` `has no server assigned` 409). No eager `ValidateCompose` on create (only later via `handlers_apphosting.go:892` `PUT /apps/:id/compose`). Gap downgraded from P2 but not closed. |
| **C02** | Deploy (rollout strategies) | `PARTIAL` + FLF-01 `P0 FALSE_COMPLETION` stubs report completed with zero containers | **AP-02** `Deploy strategies (recreate/rolling/blue-green/canary)` | `FALSE_COMPLETION (P0)` `execution.go:249` stubs | **02/03/04/05** `recreate PARITY (fixed)` `rolling PARTIAL` `blue-green PARTIAL` `canary PARTIAL` | **Current: PARTIAL (recreate honest, others unwired)** — `execution.go:268-274` `executeProvisionStep` now fails closed `if runtime==nil → error` (honest, `provision_regression_test.go:13` enforces); `recreate` is `init→provision→(health_gate)→complete` with real `ApplyDeployment`. However `executePromoteStep:277` still flips DB `active_target green↔blue` via `UpdateDeploymentConfig` versioned but **no trafficmanager/loadbalancer/service_endpoints wiring** (same as FLF-08); `executeScaleUp/Down:313` + `executeDrain*:299` only `verifyObservedRunning` boolean not replica-count; `canaryPercent:25` stored but ignored in `stepsForStrategy:48`. So AP-02 stub → honest for `recreate` only, blue-green/canary/rolling remain `FALSE_COMPLETION` for traffic/weight. |
| **C03** | Start/Stop/Restart (per-app & per-service) | `UNWIRED`/`BROKEN` — app start/stop desired_state only, no reconciler; app restart → `TriggerDeploy(empty image)`; admin compose restart missing, user route alive | **AP-03** `Per-app vs per-service start/stop/restart` | `BROKEN (P1)` `handlers_apphosting.go:450` desired_state only; restart is `TriggerDeploy:500` | **06** `Start/stop/restart per-app (app-level)` | `BROKEN→PARTIAL FIX` start/stop still lie, restart fixed via `SendPower` | **Current: PARTIAL FIX (1 of 3 closed, 2 still BROKEN)** — `handlers_apphosting.go:500-537` `POST /apps/:id/restart` **FIXED**: now `ServerControlTarget` + `daemon.SendPower(powerCtx, nodeURL, nodeToken, serverID, "restart")` 60s timeout, honest `502` on failure (`handlers_apphosting.go:533`). Per-instance lifecycle `handlers_apphosting.go:444-446` + `instanceLifecycleHandler:1080` remains `PARITY` (validates `GetServiceStatus` + `GetInstance` `AppID==appID` + `daemon.AdminContainer{Start,Stop,Restart}`). **STILL BROKEN:** `POST /apps/:id/start:450` + `stop:475` still do `UpdateApp{DesiredState:"running"|"stopped"}` (`store_apphosting.go:313`) with **no actuation, no reconciler, no `UpdateApplicationStatus`**, return `{ok:true}` immediately. No `reconciler` watches `applications.desired_state != observed_status` (only `replicamanager/reconcile:732` watches `ReplicaApplications`). Frontend badge stale `idle`. Composition admin `POST /compose/:id/restart:470` **now exists** (see C03 gap 3 fix) but `forge/web/lib/api/compose.ts:88-98` still **no `restartComposeStack` export** — wired at API, unwired at web. |
| **C04** | Update / Redeploy | `PARTIAL` — compose update substantive with rollback; app update metadata-only split brain; compose redeploy creates new row leak | **AP-04** `Hash+env diff dedup (no-op deploy skip)` | `COMPLETE` `service.go:594` hash compare | **08** `Update/redeploy – hash+env diff deduplication` | `PARITY` hash+env short-circuit `lifecycle.go:492` vs **09** `rollbackStack` `PARITY` | **Current: FIXED (row leak closed)** — `handlers_compose.go:413-438` `POST /compose/:id/deploy` now calls `UpdateComposeStack` (in-place with rollback+health) not `DeployComposeStack` fresh `cps-` ID. Comment cites `Phase-1 F-08` fix `line 423-425`. `lifecycle.go:492-494` `newHash==oldHash && mapsEqual(env)` short-circuit plus `computeHash:988` SHA256 prevents noisy redeploys. **Remaining gap:** `UpdateComposeStack` has no `ValidateCompose` call (API-level security validated only at `DeployComposeStack:264` not at `UpdateComposeStack` entry), so `PATCH /compose/:id` can store privileged YAML until beacon rejects with 400 → hash DB already mutated to `updating` before rollback (`F-10` still open). App path: `PUT /apps/:id` (`handlers_apphosting.go:231`) still metadata-only, requires separate `POST /apps/:id/deploy:282` `TriggerDeploy` (two-step silent stale version). No `?redeploy=true` atomic. |
| **C05** | Rollback | `PARTIAL` — image-only, version race FLF-04 | **AP-05** `RollbackStack / version guards` | `BROKEN` `appstore/service.go:158` always upgrades to latest, ignores `app_ignore_upgrade` | **13** `Revisions/history/compare` | `PARITY` with LF-05 race | **Current: STILL BROKEN (race & scope) + AP-05 appstore guard ignored** — `revisions.go:147` `RollbackToRevision` does `UpdateDeploymentStatusVersioned:167` (bumps version V→V+1) then **non-version-checked** `UpdateDeployment:174` `toStoreDeployment(deployment)` where in-memory `deployment.Version` is still stale V (pre-bump) → second `UPDATE deployments SET ... version=version+1 WHERE id=$1` (no `WHERE version=`) (`store_deployments.go:128-137`) silently overwrites. `CreateRevision:82-91` increments `nextNum = latest.RevisionNumber+1` after `GetLatest` **without TX/SELECT FOR UPDATE** → concurrent `executeInitStep` can duplicate `RevisionNumber`. History fragmented: `deployment_revisions` is per-`deployment_id`, not per-`application` nor per-`compose_stack` — each `TriggerDeploy` starts at revision 1, cannot list "all revisions for app X" without joining `applications.current_deployment_id` (latest only). Compose `rollbackStack:844` restores YAML but API path via generic `RollbackToRevision` restores only `ImageRef`. |
| **C06** | Delete / Uninstall | `PARTIAL` — no guard for active deploy, no orphan sweep | **AP-06** `Delete cascade / orphan` | `BROKEN` `apphosting/service.go:175` unconstrained; `handlers_compose.go:413` leaks row | **10** `Delete/cascade` | `BROKEN` `DeleteApplication` unconstrained `DELETE WHERE id` cascades `app_services` but not `instances/reservations` | **Current: STILL BROKEN (fence + orphan)** — `apphosting/service.go:175-183` `DeleteApp` only checks `AppBelongsToOrg`, no `isActiveStatus:119` guard, no `CancelReservation`, no `replicamanager.DeleteApp:501` sweep (which iterates instances + `Reservations.CancelReservation` + `DeleteInstance`). `store_apphosting.go:374` `DELETE FROM applications WHERE id=$1` FK `ON DELETE CASCADE` for `app_services` remains, but `instances` via `replica_app_id` orphaned plus `BeaconCommandLog` never cleaned. `DeleteComposeStack:570` sets `deleting→deleted`, calls `daemon.ComposeDelete:599` `docker compose down -v` (conditionally `--remove-orphans` only if `?removeOrphans=true` default false `compose.go:574`), so old subnet/VOL `removeOrphans=false` leaves orphans. App delete never tells beacon. **Partially fixed:** `compose DeleteComposeStack` now cancels reservation `608`, but `apphosting DeleteApp` never calls it. |
| **C07** | Scale / Replicas | `PARTIAL` — silent no-op when `ReplicaAppID==nil`; no autoscaler HPA | **AP-07** `Scale / replicas + autoscaler binding` | `BROKEN (P1)` `handlers_apphosting.go:416` silent no-op when `ReplicaAppID==nil` | **11** `Scale/replicas + autoscaler binding` | `PARTIAL → FIXED ADMISSION` `ScaleService:432` refuses when `ReplicaAppID==nil` | **Current: ADMISSION FIXED, split-brain remains** — `apphosting/service.go:432-433` now `if targetReplicas>0 && existing.ReplicaAppID==nil → "scale refused: service has no replica placement"` (honest). `handlers_apphosting.go:416-439` `PATCH .../services/:serviceId` now after `UpdateService` checks `if req.Replicas!=nil && updated.ReplicaAppID!=nil && cfg.ReplicaManager!=nil → ReplicaManager.ScaleApp:434`. `replicamanager/service.go:361` `ScaleApp` honors generation via `PlaceReplicas`/`ScaleReplicas`+ per-shard locks `appLocks[64]`. **Remaining:** `ScaleService:440` does DB `UpdateAppService(replicas)` **before** `UpdateReplicaAppReplicas:446` — if `ReplicaManager.ScaleApp` fails after DB, DB shows scaled `replicas` with no placement (split-brain). No autoscaler binding for app replicas: `autoscaler/service.go:332` `ResizeServer` scales `servers` vertical Memory/CPU, not horizontal replica count — HPA missing per `C07` P2. |
| **C08** | Health / Probes / Health Gate | `PARTIAL` + FLF-05 `P0` localhost-only gate always hits API not node | **AP-08** `Health gates (http vs ps)` | `BROKEN (P0)` `healthgate.go:11` probes `localhost` + `validateHealthGateTarget:71` clamps to loopback | **12** `Health gates – http vs ps` | `DIVERGED (stronger)` split http (deployment) via `resolveNodeHost` + ps (`WaitForHealthy`) | **Current: CORE FIX LANDED, refinement remains** — `healthgate.go:12-30` now `host = deployment.HealthCheckHost || resolveNodeHost(ctx, ServerID)` (`resolveNodeHost:62` parses `ServerControlTarget NodeURL` → `Hostname()`). Explicit `HealthCheckPath==""||Port==0 → Passed:true (unchecked gate)` (`healthgate.go:13`). `validateHealthGateTarget:71-89` still rejects non-loopback `HealthCheckHost` when caller **explicitly** sets `HealthCheckHost` (error `health checks may only target the local deployment gateway` `rollout.go:87`), so explicitly passing node IP `10.0.0.5` via `HealthCheckHost` is blocked — intentional: default path must leave `Host==""` to get node-derived host. `WaitForHealthGate:79-125` 5s ticker `intervalMs` default 5000, threshold 3 consecutive successes over `TimeoutSeconds` default 300. Compose health stays `ps` semantics `lifecycle.go:188` `WaitForHealthy` polls `daemon.ComposeStatus` `State==running||Status==up` + restart-count `+1` threshold `213-223` — no `docker inspect Health` native. `health_check_configs` table still disjoint from deployment gate. |
| **C09** | Version / Revisions / History / Diff | `PARTIAL` — per-deployment not per-app, configHash 12hex half, excludes env/ports | **AP-09** `Revisions/history/compare` | `PARTIAL` Git vs placement revision stores disjoint | **13** `Revisions/history/compare` | `PARITY` (with LF-05) | **Current: PARTIAL (audit confusion remains)** — `revisions.go:58` `configHash` SHA256 JSON `hex[:12]` still truncated; `CreateRevision:82` still increments from `GetLatest` without tx; rollback race per C05. `store_deployment_revisions` `UNIQUE(deployment_id, revision_number)` keeps fragmentation per deploy. `CompareRevisions:225-268` diffs 5 fields correctly (`imageRef,composeManifestRef,gitCommitSha,configHash,metadata`). `execution.go:210` `executeInitStep` snapshots `RevisionConfig{ImageRef, Description}` correctly and sets `active` + `UpdateDeploymentCurrentRevision`. Cannot answer "list app history" without joining across deploys. |
| **C10** | Logs (deploy + container + compose) | `PARTIAL` — build logs only for current deployment, no streaming | **AP** (ru→RT) not in AP section but in Runtime RT-05 | `PARTIAL` one-shot 512KB, no WS stream | **Subagent-02 not scored** but lifecycle logs mapping exists | **Current: PARTIAL unchanged** — `handlers_apphosting.go:578-604` `GET /apps/:id/logs` still `ListDeploymentBuildLogs` via `currentDeploymentID` faux `BuildLog{stage,message,createdAt}` → no runtime `docker logs`; polling 5s in `[id]/page.tsx`, no websocket `docker logs -f`. `lifecycle.go:663` `GetStackLogs` → `daemon.ComposeLogs:131` → `beacon compose.go:661` locks per stack, runs `docker compose logs --no-color --tail {tail} [service]` plain text 30s timeout, 1 MB limit `daemon/compose.go:150`. Deleted `compose.yaml` → logs disappear. No change since Phase-1. |
| **C11** | Status / Desired vs Observed / Reconciliation | `FALSE_COMPLETION` observed_status write-only stub, no controller | **AP-10** not in AP-18 list (merged into AP-03) but `FINAL_PARITY` mentions reconcile missing | BROKEN ledger | **06** noted no reconciler | **Current: STILL FALSE_COMPLETION for app desired_state** — `store_apphosting.go:11` `Application{DesiredState,ObservedStatus}` `ObservedStatus:22` defaults `idle` at `CreateApplication:167`; `UpdateApplicationStatus:379` exists but only caller is `apphosting/service.go:194` manually; no `AppReconciler` loop. `handlers_apphosting.go:450/475` `UpdateApp{DesiredState}` never bumps `observed_status`. Frontend `fetchApps → statusBadge` reflects stale `idle`. `compose_stacks.status` is lively (`deploying→awaiting_health→running|degraded→deleting`) but lacks periodic poll for external `docker kill` drift. `replicamanager/service.go:732` `reconcile` only watches `ReplicaApplications`, not `applications`. |
| **C12** | Queue / Execution / Concurrency / Idempotency | `PARTIAL` — lease path correct, compose path sync+no lease | Not in AP-18 list (covered by Orchestration) | Orchestration queue findings | **Subagent-02 ignores queue** but deployment lease present | **Current: STILL PARTIAL (lease fence + compose bypass)** — `execution.go:22-35` `ClaimExecutionLease` 5m + unconditional `defer ReleaseExecutionLease:32` (see Finding 4 below). Compose `DeployComposeStack:249` remains synchronous inline `daemon.ComposeDeploy` + `WaitForHealthy` 2m, not enqueued via `queue.Service` (deprecated `operation/service.go:48` `OpCompose*` pointer notes). Partial index `idx_unique_active_deployment_per_server:106` still guards one active per server, but `DeleteApp` bypasses. |
| **C13** | Compose Spec Control / Validation | `PARTIAL` — dual-wall drift, warn vs block | **AP-10** `Env interpolation + env_file` | `BROKEN (P0)` env_file dropped | **14** `Compose env interpolation + env_file` | `PARTIAL (env_file missing)` | **Current: PARTIAL unchanged, env_file silently dropped** — `compose/service.go:240` `ValidateCompose` + `interpolateEnv:352` handles `${VAR}`, `${VAR:-d}`, `${VAR-d}`, `${VAR:?err}`, `$$` correctly; `beacon/compose.go:77` `validateComposePolicy` duplicates check. `env_file/include` still dropped: `rawService.Environment` parsed, but `ParseComposeYAML` `loader.LoadWithContext Environment:map[string]string{}` empty (`parser.go:197` per Subagent-02), beacon `.env` only from `EnvVars` map `compose.go:380`. `healthcheck/deploy` still warning-only at API, ignored at beacon. |
| **C14** | Zero-downtime / Blue-green / Canary / Rolling | `UNWIRED` (promotion/traffic) + FALSE_COMPLETION FLF-08 | **AP** merged into AP-02 strategies | FALSE_COMPLETION | **02-05** noted wiring gap | **Current: UNWIRED traffic still** — see C02. |
| **C15** | Preview / PR Deployments | `PARTIAL` metadata-only URL synthesis | **AP-15** `Preview envs (per-PR)` | `DUPLICATE` hard-coded `preview-<suffix>.example.com:125` vs `previewenv TTL` | **18** `Catalog/AppStore/Preview/Build` | `DUPLICATE/UNWIRED` | **Current: UNCHANGED DUPLICATE** — `preview/service.go:42` `Create` `IsIsolated=true` + `Deploy:125` synthesizes `https://preview-{{suffix}}.example.com` without reserving `proxy_domains`; `previewenv/service.go:121` TTL+MaxPerOrg not on admin namespace `phase4_registrar.go:37`. No real ingress `Caddy proxy`. |
| **C16** | Backup & Restore (app-level) | `PARTIAL` — sync `ExecuteBackupJob` inline, status enum drift | Not in AP section (Backup audit) | PARTIAL | — | **Current: UNCHANGED** — `handlers_apphosting.go:736` `appBackupSvc.ExecuteBackupJob` synchronous in handler (Fiber default timeout risk), status drift frontend `pending\|creating\|completed` vs backend `preparing\|downloading\|...`. No pre-update snapshot hook for `UpdateComposeStack`/`Rollback`. |
| **C17** | Multi-service & Dependencies | `PARTIAL` — stores depends_on but never enforces ordering | **AP-11** `Service dependencies / convergence` | `PARTIAL` parser strings, runtime delegates to `docker compose` | **15** `Dependencies/convergence` | `PARTIAL` `depends_on` normalized `service.go:764` but not ordered | **Current: UNCHANGED** — `apphosting/service.go:198` stores `Ports/EnvVars/DependsOn JSONB`; `replicamanager` placement ignores graph (fan-out `deployReplicas` unordered). Acceptable for `compose` path (`docker compose up -d` does topological sort), but app-services path should topologically sort via `InDependencyOrder:78` before `DeployApp`. |
| **C18** | Source Types (GIT/DOCKER_IMAGE/COMPOSE) | `PARTIAL` — SourceType immutable, git ls-remote not credential-aware | **AP-01** mixed in + **GB** section (not AP) | — | **01** `App create` `source_type` immut | **Current: UNCHANGED** — `applications.source_type CHECK` immutable after create; `UpdateApp` does not allow `SourceType` migration (Coolify allows switch). `handlers_apphosting.go:1046` `GET /admin/git-branches` `git ls-remote --heads` on API host `exec git` not credential-aware (private repos fail even if `CredentialID` stored). Git webhook `handlers_compose.go:83` + `SetAutoUpdate` polling dual-trigger duplicate not deduplicated. |

**Reconciliation summary:** Phase-01 identified 3 `FALSE_COMPLETION` (deploy stubs, desired vs observed, Z-D promote) + 9 `PARTIAL` + 2 `UNWIRED` + 1 `BROKEN` + 3 `MISSING`. FINAL_PARITY_AUDIT re-labeled top defects as 4 `FALSE_COMPLETION`/`BROKEN` clusters (`AP-02 P0`, `AP-03 P1`, `AP-05/06 BROKEN`, `AP-07/08 P0/P1`, `AP-10 P0`). Final-parity subagent-02 split strategies into 4 rows and upgraded `recreate` to `PARITY (fixed)` while keeping rolling/blue-green/canary `PARTIAL`, and downgraded health to `DIVERGED (stronger)` after `resolveNodeHost`. Re-verification confirms: **3 fixes landed honestly** (restart via power `handlers_apphosting.go:500`, deploy health node host `healthgate.go:23`, redeploy row-leak `handlers_compose.go:413`, scale admission `apphosting/service.go:432`), **5 close remains** (`start/stop` lie `C11`, `delete` fence `C06`, `rollback race` `C05`, `volume bifurcation` `AP-13`, `env_file` `C13`).

---

## 3. Detailed Re-inspection — 20 Row Parity Matrix (≥15 required)

Columns: **ID | Capability | Reference file:line | Forge stack (Frontend : API : Service : Store : DB : Beacon : Worker/Event/Tests) | Re-verified Status | Gap / Finding ID | Severity**

| # | Capability | Reference file:line | Forge stack (file:line evidence) | Status | Gap / Finding | Severity |
|---|------------|---------------------|----------------------------------|--------|---------------|----------|
| 01 | **App create – source types & admission** | Coolify `app/Models/Application.php:118` `class Application` + CapRover `src/models/ICaptainDefinition.ts:1` `ICaptainDefinition` | FE `forge/web/lib/api/apps.ts:243` `createApp POST /apps` → API `forge/api/internal/http/handlers_apphosting.go:173` `POST /apps` `handlers_apphosting.go:100` defaults `DOCKER_IMAGE` → Service `forge/api/internal/services/apphosting/service.go:92-122` `CreateApp` `validSourceTypes:81` → Store `forge/api/internal/store/store_apphosting.go:151` `CreateApplication` `desired_state running observed idle` → DB `migrations/100_z_app_platform_applications.sql:5` `applications(org_id, source_type GIT\|DOCKER_IMAGE\|COMPOSE, desired/observed)` → Tests `service_test.go` shallow | **PARTIAL** | `SourceType` defaults silently to `DOCKER_IMAGE` when empty (masks caller error vs CapRover exactly-one `ImageMaker.ts:416`); `server_id` nullable on create → `idle` forever until `TriggerDeploy:695` `has no server assigned`. No eager `ValidateComposeSecurity` at create. | P2 `OPERATOR_VISIBLE` |
| 02 | **Deploy recreate (baseline)** | Komodo `bin/core/src/api/write/stack.rs` `DeployStack → periphery compose up -d` + `docker-compose/pkg/compose/convergence.go:78` | FE `forge/web/lib/api/deployments.ts:128` `executeDeployment` → API `forge/api/internal/http/handlers_deployment.go:68` `POST /:id/execute` → Service `forge/api/internal/services/deployment/execution.go:22` `ExecuteDeployment` `steps.go:50` `[init,provision,(health_gate),complete]` `execution.go:264` `executeProvisionStep` real `ApplyDeployment` → Store `forge/api/internal/store/store_deployments.go:300` `ClaimExecutionLease` 5m | **PARITY (FIXED)** | Honest failure when `runtime==nil` (`execution.go:269` `no runtime executor wired`) + `provision_regression_test.go:13` enforces. Previously `FALSE_COMPLETION` closed. No traffic concern for recreate. | Low |
| 03 | **Deploy rolling** | Coolify `app/Jobs/ApplicationDeploymentJob.php:1904` `rolling_update` + Uncloud `pkg/client/service.go:24` fan-out `Deployment.Run` | API `forge/api/internal/http/handlers_revisions.go:48` `POST /:id/rollout StrategyRolling` → Service `forge/api/internal/services/deployment/rollout.go:181` `rollingRollout` `steps.go:57` `[init,scale_up,(health_gate),scale_down,complete]` → `execution.go:313` `executeScaleUpStep:verifyObservedRunning:338` `VerifyRunning(ctx, ServerID) bool` | **PARTIAL** | `verifyObservedRunning` is **boolean** not replica-count vs `TargetReplicas`/`Replicas` (`execution.go:338` `if !running → fail`). Rolling 10-replica app where 9 running passes incorrectly; `TargetReplicas` defaulted to 1 via `applyRolloutRequest:112` clobber if caller omits (LF-02 still open). No per-replica % or `MaxUnavailable`. | **P1** `USER_VISIBLE` LF-01 |
| 04 | **Deploy blue-green + traffic** | CapRover `ServiceManager.ts:888` `ensureServiceInitedAndUpdated` Swarm update | Service `forge/api/internal/services/deployment/rollout.go:233` `blueGreenRollout` `service.go:276` `StartBlueGreen` `BlueTargetID=server-blue GreenTargetID=green-%d` `steps.go:64` `[init,provision,(health_gate),promote,(verify),drain_old,complete]` → `execution.go:277` `executePromoteStep` toggles `active_target` DB only via `UpdateDeploymentConfig` versioned | **UNWIRED / FALSE_COMPLETION (traffic)** | `executePromoteStep` publishes `deployment_promoted` but never reconfigures `trafficmanager`/`loadbalancer`/`service_endpoints`/`target_groups` or `docker stop` old. Both blue+green containers keep same host port → port collision on compose. | **P1** `USER_VISIBLE` |
| 05 | **Deploy canary (weighted)** | Uncloud staged placement | Service `forge/api/internal/services/deployment/rollout.go:261` `canaryRollout` `RolloutRequest.CanaryPercent:25→10 default` `steps.go:75` `[init,provision,(health_gate),drain_canary,promote,complete]` | **PARTIAL** | `canaryPercent` stored and event-published (`canaryRollout:305` `canaryPercent`) but `stepsForStrategy` ignores it — same DAG for 1% and 99% canary; `StepDrainCanary:306` is `verifyObservedRunning` boolean. No incremental weight via `replicamanager.ScaleApp` subsets. | P2 `OPERATOR_VISIBLE` |
| 06 | **Per-app start / stop (desired vs observed)** | Dokku `plugins/ps/ps.go:148` `Restore/Start/Stop` via `scheduler-stop`; 1Panel `agent/app/service/app_install.go:246` `Operate(start\|stop)` → `compose.Up/Stop` + `SyncAppInstallStatus:835` | FE `forge/web/lib/api/apps.ts:255` `startApp POST /apps/:id/start` `apps.ts:259` `stopApp` → API `forge/api/internal/http/handlers_apphosting.go:450` `POST /apps/:id/start` `handlers_apphosting.go:468` `UpdateApp{DesiredState:"running"}` → Service `forge/api/internal/store/store_apphosting.go:313` `UpdateApplication` dynamic SET → Store `UpdateApplicationStatus:379` never called by reconciler | **BROKEN (FALSE_COMPLETION)** | Writes `desired_state` only, **no reconciler** watches `desired_state != observed_status`. `observed_status` stays `idle` forever unless `UpdateApplicationStatus:194` manually. API returns `{ok:true}` immediate, frontend badge stale. Replicamanager `reconcile:732` only for `ReplicaApplications`. | **P0** `USER_VISIBLE` LF-03 |
| 07 | **Per-app restart (power vs deploy)** | Dokku `plugins/ps/ps.go:91` `Restart:deploy(imageTag)`; 1Panel `Operate{restart}` `docker compose restart` | FE `forge/web/lib/api/apps.ts:263` `restartApp POST /apps/:id/restart` → API `forge/api/internal/http/handlers_apphosting.go:500-537` `POST /apps/:id/restart` → `ServerControlTarget` → `daemon.SendPower(powerCtx NodeURL NodeToken ServerID "restart")` 60s timeout `handlers_apphosting.go:533` | **PARITY (FIXED)** | Previously `TriggerDeploy(empty image)` `apphosting/service.go:666` → `validateImageRef:322` `@sha256:` fail + spurious history row. Now routed through canonical beacon power channel honest `502 node restart failed`. Separate from deployment pipeline. | Low (integration) |
| 08 | **Per-service instance lifecycle** | Portainer `api/docker/container.go` `ContainerService.Recreate:55`; Uncloud per-container `RunService` | API `forge/api/internal/http/handlers_apphosting.go:444-446` `POST .../instances/:instanceId/{start,stop,restart}` → `instanceLifecycleHandler:1083` `GetServiceStatus` org check + `GetInstance:1102` `instance.AppID==appID` guard + `GetNode:1111` `GetNodeDaemonCredential:1119` → `daemon.AdminContainer{Start,Stop,Restart}:1139-1143` + `UpdateInstanceStatus` + audit | **PARITY** | Correct fan-out to beacon; validates `AppServiceBelongsToApp` chain. Gap: cross-check `Instance.AppID` is `ReplicaAppID` not `Application.ID` → theoretical cross-app access when IDs alias but mitigated by prior `GetServiceStatus` org guard. | Medium |
| 09 | **Update / redeploy hash+env dedup** | Coolify `app/Models/Application.php:1051` `revision` hash + `config_diff` | API `forge/api/internal/http/handlers_compose.go:413` `POST /compose/:id/deploy` → Service `forge/api/internal/services/compose/lifecycle.go:476` `UpdateComposeStack` `computeHash:988` SHA256 `mapsEqual:993` `newHash==stack.ComposeHash && mapsEqual(env)` short-circuit `lifecycle.go:492` | **PARITY (FIXED)** | Row-leak fixed: private handler now `UpdateComposeStack` in-place (`handlers_compose.go:427` comment cites `F-08`) not `DeployComposeStack` fresh `cps-%d`. User surface `handlers_user_console.go:135` `POST /user/compose/:id/deploy` still `DeployComposeStack` fresh row (user duality) — minor remaining leak. | Low |
| 10 | **Rollback file + health-observation** | Komodo `periphery compose.rs:713` `destroy_before_deploy` + 1Panel `.env` backup rollback | Service `forge/api/internal/services/compose/lifecycle.go:844` `rollbackStack` captures `rollbackHash/YAML/Env:497-499`, on `ComposeDeploy` error `lifecycle.go:539` → `rollbackStack` re-deploys + `WaitForHealthy:883` + `markFailed:837`; `DeleteComposeStack:570` `down -v` with `removeOrphans` opt-in false default `compose.go:301` doc | **PARITY** | `rollbackStack` swallows `GetNode/DaemonCredential` errors with `markFailed` not returned to caller (`lifecycle.go:855`), so UI sees generic `deploy updated stack: <error>` not rollback cause. Hash mutation before beacon `400` leaves DB divergent briefly before rollback. | Medium |
| 11 | **Delete / cascade / orphan sweep** | Coolify `DeleteResourceJob` cascades vols/nets; `Dokku TriggerAppDestroy` retires containers; 1Panel `DeleteCheck:692` | API `forge/api/internal/http/handlers_apphosting.go:259` `DELETE /apps/:id` → Service `forge/api/internal/services/apphosting/service.go:175` `DeleteApp` → Store `forge/api/internal/store/store_apphosting.go:374` `DELETE WHERE id` | **BROKEN** | No `isActiveStatus:119` fence on `deployments.status pending\|in_progress\|provisioning\|awaiting_health\|promoting`; no `placement reservation CancelReservation` (`lifecycle.go:608` only on compose); no `replicamanager.DeleteApp:501` iteration `StopInstance+CancelReservation+DeleteInstance`; beacon containers `docker compose down -v` never called for app path; `instances` orphaned via `replica_app_id`. | **P1** `OPERATOR_VISIBLE` LF-04 |
| 12 | **Scale / replicas + placement** | Dokku `ps:ps.go:57` `Formation`; CapRover `instanceCount`; Uncloud `Replicas` wg | API `forge/api/internal/http/handlers_apphosting.go:416` `PATCH .../services/:serviceId` `UpdateService:427` then `if Replicas!=nil && ReplicaAppID!=nil → ReplicaManager.ScaleApp:434` → Service `forge/api/internal/services/apphosting/service.go:398` `ScaleService` `ReplicaAppID==nil → scale refused:433` `UpdateReplicaAppReplicas:446` + `forge/api/internal/services/replicamanager/service.go:361` `ScaleApp` generation+64-shard locks | **PARTIAL (admission fixed, tx split)** | Silent no-op fixed to honest refusal. Remaining: DB `UpdateAppService(replicas)` happens **before** `UpdateReplicaAppReplicas` / `ReplicaManager.ScaleApp`; failure after DB leaves split-brain (`ScaleService:440-450` returns `updated` even if replica update fails `non-fatal`). Compose per-service `POST /compose/:id/scale` still absent despite `create.go:86` `--scale`. No HPA binding for `app_services` (autoscaler `ResizeServer` vertical only). | **P1** admission fixed, **P2** compose scale |
| 13 | **Health gates (deployment http + compose ps)** | Dokku `plugins/checks/report.go:7`; Coolify `isRunning:792` dual `status:health`; Komodo `monitor/mod.rs:48` `PollStatus` | Service `forge/api/internal/services/deployment/healthgate.go:11` `CheckHealth` `host=(explicit||resolveNodeHost:62)` `http://host:port/path` 10s `200-399` pass `healthgate.go:50` + `WaitForHealthGate:79` ticker 5s threshold 3 consecutive + Service `forge/api/internal/services/compose/lifecycle.go:188` `WaitForHealthy` `ComposeStatus` `State running && Status up` + restartCount `+1` guard `lifecycle.go:213` | **DIVERGED (stronger split)** | Deployment http gate no longer hits `localhost` (fixed `resolveNodeHost` parse `ServerControlTarget NodeURL→Hostname:73`). Gate returns `Passed:true` silently when `Path==""||Port==0` (`healthgate.go:13`); compose health is `ps` not `docker inspect Health`. `validateHealthGateTarget:71` clamps explicit `HealthCheckHost` to empty or loopback — correct for default but blocks caller trying to pass node IP explicitly (must leave empty). `health_check_configs` table still duplicate / disjoint. | Medium |
| 14 | **Revisions / history / compare / version** | Coolify `queue per app SHA`; Dokku `git SHA deployed_version`; CapRover `versions[] maxVersionHistory` | API `forge/api/internal/http/handlers_revisions.go:16` `GET /:id/revisions` `32` `POST .../rollback` `64` `compare?from=&to=` → Service `forge/api/internal/services/deployment/revisions.go:82` `CreateRevision` `147` `RollbackToRevision` `225` `CompareRevisions` `58` `configHash 12hex` → Store `forge/api/internal/store/store_deployment_revisions.go` | **PARTIAL** | `CreateRevision` non-transactional `GetLatest→nextNum+1` race duplicate `RevisionNumber` (violates `UNIQUE(deployment_id,revision_number)` under concurrency). `RollbackToRevision:167` `UpdateDeploymentStatusVersioned(V→V+1)` then non-versioned `UpdateDeployment:174` stale `Version` clobber (LF-05). Per-deployment numbering restarts at 1 per new `deployments` row — no linear app history. `configHash` 12hex collisionable, excludes `EnvVars/ports/resources`. | **P1** LF-05 |
| 15 | **Queue / concurrency / lease / idempotency** | Coolify Horizon per-app queue tags; Komodo per-stack `compose.go:39` lock | Service `forge/api/internal/services/deployment/execution.go:22` `ClaimExecutionLease 5m:24` vs Store `forge/api/internal/store/store_deployments.go:300` `claim` + `store_deployments.go:342` `ReleaseExecutionLeaseIfOwner` exists but **unused**; `execution.go:32` `defer ReleaseExecutionLease unconditional` + `execution.go:490` `resumeFromStep` same; `rollout.go:129` `isActiveStatus` + `migrations/106` `idx_unique_active_deployment_per_server` | **BROKEN (lease fence)** | Unconditional release violates fencing: if lease expires mid-long-step (30s `handleStepFailure:112` + 5m default) second worker claims, first defer wipes second lease → third can claim double-execute `Promote`. Compose `DeployComposeStack:249` remains sync inline bypassing `queue.Service` (deprecated `operation/service.go:48` `OpCompose*` pointer). No lease renewal heartbeat. | **P1** `OPERATOR_VISIBLE` LF-07 |
| 16 | **Compose spec validation dual wall** | Coolify `compose_parsing_version:331` + CapRover strict allowlist; Portainer `ValidateComposeSecurity` | Service `forge/api/internal/services/compose/service.go:240` `ValidateCompose` (`service.go:427` `ValidateComposeSecurity:privileged,pid,cap_add,devices,ports<1024,bind mounts`) + `forge/api/internal/services/compose/lifecycle.go:264` API pre-check before node + Beacon `beacon/internal/server/compose.go:77` `validateComposePolicy` duplicate (`cap_add,network_mode,devices,ports<1024,volumes`) | **PARTIAL (bifurcated)** | API `checkVolumesSecurity` allows `/data:/data` (not in `sensitiveHostPaths:679` `["/","/root","/etc","/home"]` warning-only), while `beacon compose.go:226` rejects **any** absolute `source "/"` or `..` traversal or long-form `type:bind` hard (`compose.go:234` `bind mounts not allowed`). Valid API `200 {valid:true}` → beacon `400 compose policy violation host bind mount "/data"` confusing. `allowedMounts` allowlist via `ValidateHostMountWithAllowlist:663` never plumbed to beacon. | **P1** `USER_VISIBLE` + `OPERATOR_VISIBLE` LF-06 |
| 17 | **Update env interpolation & env_file** | Compose `envresolver.go` `loader.go` `include/env_file` | Service `forge/api/internal/services/compose/service.go:352` `interpolateEnv ${VAR:-default} ${VAR:?msg} $$:370-414` shared `ExpandTemplate` + Parser `forge/api/internal/services/compose/parser.go:197` `loader.LoadWithContext Environment:map[string]string{}` empty | **BROKEN (P0 secret loss)** | `env_file:`/`include:` silently dropped — host secret file not written; deploy proceeds with empty vars where secrets expected → runtime failure. `healthcheck`/`deploy.resources` still warning-only at API, ignored at beacon. | **P0** `USER_VISIBLE` |
| 18 | **Compose restart wiring (admin vs user)** | 1Panel `Operate{restart}` `docker compose restart`; Compose `cmd/compose/compose.go:561` `restartCommand` → `pkg/compose/restart.go` | Beacon `beacon/internal/server/compose.go:506` `handleComposeRestart` `docker compose restart` per-stack lock `compose.go:39`; API admin `forge/api/internal/http/handlers_compose.go:470` `POST /compose/:id/restart → RestartStack:781=StopStack→StartStack` NEW; API user `forge/api/internal/http/handlers_user_console.go:231` `POST /user/compose/:id/restart` → same; Daemon `forge/api/internal/daemon/compose.go:81` `ComposeRestart`; FE `forge/web/lib/api/compose.ts:88` `deployComposeStack` `93` `stopComposeStack` `98` `startComposeStack` **no** `restartComposeStack` | **WIRED at API, UNWIRED at Web** | Admin restart gap closed at control-plane (`REF-APP-C03 F-06` fixed), but UI cannot reach it: no client export, `forge/web/app/admin/compose/page.tsx` actions `stop/start/delete` only (`page.tsx:44`), `[id]/page.tsx` `stop/start/redeploy` no restart. `RestartStack:781` is `StopStack→StartStack` sequential without atomicity/reservation — `StopStack` success + `StartStack` failure after concurrent `UpdateComposeStack` race leaves `failed` without rollback. | P1 `USER_VISIBLE` |
| 19 | **Scale wiring parity (admin compose vs per-service)** | Compose `create.go:86` `--scale SERVICE=NUM` `applyScaleOpts` | Admin compose `408` quotas `MemoryMB,CPUShares,DiskMB` but no per-service `replicas` knob; `lifecycle.go:697` `StartStack` requires `StackStatusStopped` check `703` `ErrStackNotRunnable` | **MISSING** | No `POST /compose/:id/scale` despite `beacon handleComposeRestart` infra; compose services scale only via spec edit. | P2 `OPERATOR_VISIBLE` |
| 20 | **Logs / health observability wiring** | Coolify `ApplicationDeploymentJob` stdout → DB + live Tailwind; Komodo `StackLogs` periphery `logs --tail` | FE `forge/web/lib/api/apps.ts:279` `fetchAppLogs GET /apps/:id/logs` `forge/web/lib/api/compose.ts:105` `getComposeStackLogs(service?,tail?)` → API `forge/api/internal/http/handlers_apphosting.go:578` `ListDeploymentBuildLogs` current only → Service `forge/api/internal/services/compose/lifecycle.go:663` `GetStackLogs → daemon.ComposeLogs:131 → beacon compose.go:661` `docker compose logs --no-color --tail` 30s + 1 MB cap | **PARTIAL** | Build logs only current deployment faux `BuildLog{stage,message}` → no `docker logs -f` live runtime; absentee `instances/:instanceId/logs` for non-compose; no WebSocket tail; `ComposeLogs` requires `compose.yaml` exists (after `DeleteComposeStack:596` `os.RemoveAll` logs vanish). Polling only, `states-empty` vs rate-limit mismatch inherited. | P3 `OPERATOR_VISIBLE` |

**Counts (current reverified):** `PARITY (fixed)` 4 (02 recreate, 07 restart, 08 instance lifecycle, 09 hash-dedup+rollbackStack) + `PARTIAL` 9 + `UNWIRED/BROKEN` 4 + `MISSING` 1 + `DIVERGED` 2 = **20 rows**.

---

## 4. Focus Questions — Verified Answers

Each question answered with **file:line evidence + status after Phase-06 fixes**.

### AP-02 — Do `execution.go:249` stubs still report `FALSE_COMPLETION`?

**Answer: PARTIALLY FIXED — recreate honest, blue-green/rolling/canary/scale still stub/false-complete on traffic & replica dimensions.**

- **Before:** `execution.go:249` `executeProvisionStep: return nil` + `256 executePromoteStep` + `278 Drain` + `292 Scale*` + `306 executeCleanupStep` all `publish event + return nil`. `ExecuteDeployment:86-121` looped them and `UpdateDeploymentCompletion:135` marked `completed 100%` with zero provisioning. Tests `deployment_test.go:40` asserted `StatusCompleted` after no-ops.
- **Now:** `execution.go:264` `executeProvisionStep` checks `if runtime==nil → "no runtime executor wired; refusing to report success"` before `runtime.ApplyDeployment(ctx, ServerID, Image):271`. On failure it `return fmt.Errorf("provision step: apply to node failed")` which bubbles to `handleStepFailure:112→360` → `UpdateDeploymentFailure:364` + `auto_rollback`. `provision_regression_test.go:13-60` enforces this. `recreateRollout` is therefore **honest**.
- **Remaining stubs:** `execution.go:277` `executePromoteStep` still only flips `active_target` DB flag (`289 UpdateDeploymentConfig`) + publishes `deployment_promoted` with **no** `trafficmanager/loadbalancer/service_endpoints` reconfiguration (`execution.go:292`). `executeScaleUp:313`/`ScaleDown:320`/`DrainOld:299`/`DrainCanary:306` merely `verifyObservedRunning(ctx, ServerID) bool` (`execution.go:338`) not `ListInstancesByApp` count vs `TargetReplicas`. `executeCleanupStep:327` pure event. `canaryPercent:272` ignored. So `blue-green/rolling/canary` still **FALSE_COMPLETION** for traffic/weight despite honest provision.

**Verdict:** `AP-02` false-completion **fixed for `recreate`**, **still present** for zd/traffic paths. Keep `P0` on AP-02 but narrow to `executePromote*`/`drain`/`scale` stubs.

---

### AP-03 — Does `handlers_apphosting.go:450` start/stop still lie? Is restart still `TriggerDeploy`?

**Answer: START/STOP STILL LIE; RESTART FIXED to power-cycle.**

- **Start `handlers_apphosting.go:450-473`:** `running:="running"` → `UpdateApp(ctx, id, orgID, UpdateAppRequest{DesiredState:&running}):468` → `200 {ok:true}`. No `daemon` call, no `UpdateApplicationStatus`, no `operation` queue. `store_apphosting.go:313` `UpdateApplication` only mutates `applications.desired_state`. No reconciler watches the gap (unlike `replicamanager/reconcile:732` per-app locks + `VerifyInstance`). Caller `GET /apps/:id` reads stale `observed_status:"idle"` (`applications:21-22`). **Lie remains** — `FALSE_COMPLETION P0` (LF-03).
- **Stop `handlers_apphosting.go:475-498`:** symmetric `stopped:="stopped"` same lie.
- **Restart `handlers_apphosting.go:500-537`:** **FIXED**. Was `500 restart → TriggerDeploy{recreate, Image:""}` which failed `validateImageRef:322 @sha256` or falsely completed empty. Now `handlers_apphosting.go:500` guard `ServerID==nil → 409`, `ServerControlTarget:527` → `Daemon.SendPower(powerCtx target.NodeURL target.NodeToken ServerID "restart") 60s:533` → `502 node restart failed` on error else `200 {ok:true serverId action:restart}`. Comment `517-520` documents prior `F-12` failure. **Not a deployment** any more.
- **Compose restart duality:** Admin `handlers_compose.go:470` `POST /compose/:id/restart → RestartStack:781` **now exists** + user `handlers_user_console.go:231` `POST /user/compose/:id/restart` alive, both call `beacon handleComposeRestart:506` `docker compose restart`. Backend parity closed.

---

### AP-08 — Is health still `localhost` (`healthgate.go:11` `validateHealthGateTarget:71` loopback clamp)?

**Answer: FIXED for default; clamp retained correctly for explicit host override.**

- **Before (`phase-01 FLF-05 P0`):** `healthgate.go:11` `host="localhost"` + `rollout.go:71` `validateHealthGateTarget` clamping to `localhost|127.0.0.1|::1` else error — gate always `GET http://localhost:port/path` on API host, never node. Alive `WaitForHealthGate` never reached workload, timed out after `TimeoutSeconds` + spurious `auto_rollback`.
- **Now:** `healthgate.go:12-30` `CheckHealth` does `host = deployment.HealthCheckHost || resolveNodeHost(ctx, ServerID)` (`resolveNodeHost:62-76` reads `store.ServerControlTarget(serverID)` → `url.Parse(NodeURL).Hostname()`). On `HealthCheckHost==""` (normal deployments) probe goes to **node** where container runs. `healthgate.go:23-29` returns `Passed:false Error: health check host unresolved` if node offline (honest). `resolveNodeHost` has 10s timeout `targetCtx`.
- **Clamp retained:** `rollout.go:71-89` `validateHealthGateTarget` still rejects explicit `HealthCheckHost` values that are non-empty non-loopback non-`localhost` (`rollout.go:87` `health checks may only target the local deployment gateway`). This is **correct post-fix**: default path uses node host automatically; explicit override is only for `local gateway` cases. Passing `HealthCheckHost=10.0.0.5` must be rejected — caller should leave it empty. So AP-08 `localhost` lie **fixed** but nuance preserved.
- **Remaining:** Compose `WaitForHealthy:188` is `ps` poll (`State running && Status up` + restart-count `prev+1` threshold `213`) not `docker inspect Health`; `CheckHealth` `Path==""||Port==0 → Passed:true` (`healthgate.go:13`) silently disables gate if caller forgets port (should warn). Three health tables (`deployment health_gate_*` vs `compose WaitForHealthy` vs `health_check_configs` zt `114_e`) still disjoint.

---

### AP-05 — Does `revisions.go:147` rollback still race (`UpdateDeployment` stale `Version`)?

**Answer: YES — version race STILL PRESENT (non-version-checked write + non-transactional revision numbers).**

- **`RollbackToRevision:147-199`:** `targetRev.ImageRef` `validateImageRef:162`; `UpdateDeploymentStatusVersioned:167` `WHERE version=V` bumps to `V+1`. Then `deployment.Image=targetRev.ImageRef:171` `deployment.Status=StatusInProgress` `UpdatedAt=now:173` → `UpdateDeployment:174` `toStoreDeployment(deployment)` with in-memory `deployment.Version` still `V` (stale). `store_deployments.go:128` `UpdateDeployment` is `SET ... version=version+1 WHERE id=$1` **without** `WHERE version=` → unconditional overwrite `V+1→V+2` loses concurrency check. Second concurrent rollback's `SET image` wins with wrong base, dropping first's status change. `ErrVersionConflict` not enforced here though `UpdateDeploymentConfig/Rollback/Completion/Cancelled` all enforce `RowsAffected==0 → ErrVersionConflict` (`store_deployments.go:208,242,258,274,292`). This remains `FLF-04 P2`.
- **Revision number race:** `revisions.go:87-91` `latest, _:=GetLatestDeploymentRevision(ctx, deploymentID)` `nextNum=latest.RevisionNumber+1` **without TX/FOR UPDATE** → two concurrent `executeInitStep:210` `CreateRevision` can both read `5→6`. `UNIQUE(deployment_id,revision_number)` (`migrations/099_deployment_revisions.sql:1`) would error second insert; no retry path.
- **Scope:** `RollbackToPrevious:202` piggybacks same path via `RollbackToRevision`. `service.go:444` `Rollback(blue→green flip)` uses honest `UpdateDeploymentRollback` version-checked (`store_deployments.go:230`) — only `deployment Image` path racy.

---

### AP-07 — Does `apphosting/service.go:398` `ScaleService` still no-op when `ReplicaAppID==nil`?

**Answer: NO-OP FIXED to honest admission refusal, but post-DB split-brain remains.**

- **Before:** `PATCH .../services/:serviceId` (`handlers_apphosting.go:416`) updated `app_services.replicas` column with no placement change when `ReplicaAppID==nil`; `ScaleService` blindly `UpdateAppService(Replicas)` → `FALSE_COMPLETION`.
- **Now:** `apphosting/service.go:425-433` loads `existing:=GetAppService`, if `targetReplicas>0 && existing.ReplicaAppID==nil → "scale refused: service has no replica placement (ReplicaAppID is unset)"` (err). `handlers_apphosting.go:433-436` now syncs placement when `req.Replicas!=nil && updated.ReplicaAppID!=nil && ReplicaManager!=nil → ReplicaManager.ScaleApp:434` which increments generation + `PlaceReplicas`/`ScaleReplicas`. So DB `replicas` mutated via `UpdateAppService:440` **before** `UpdateReplicaAppReplicas:446` — if latter/`ScaleApp` fails, DB shows scaled value with no placement (split-brain). Return still `updated` with `non-fatal` wrapper `service scaled but replica app update failed:448`. Autoscaler still not HPA-bound.

---

### AP-06 — Does `apphosting/service.go:175` `DeleteApp` still orphan without fence?

**Answer: YES — STILL UNCONSTRAINED + ORPHAN.**

- `apphosting/service.go:175-183` `DeleteApp` only `AppBelongsToOrg` check → `store.DeleteApplication(ctx, appID):183`. No `isActiveStatus:119` guard (`pending/in_progress/provisioning/awaiting_health/promoting/rollback_*`) unlike `rollout.go:129` used in rollout creation. Can delete while `ExecuteDeployment` loop still running `handleStepFailure:112` 30s `handleCtx` → `UpdateDeploymentFailure` on row whose `applications.server_id` now `NULL/deleted`.
- `store_apphosting.go:374` `DELETE FROM applications WHERE id=$1` cascades `app_services` via FK `ON DELETE CASCADE` (implicit) but **not** `instances` indexed by `replica_app_id` nor `placement_reservations` nor `BeaconCommandLog`. `replicamanager/service.go:501` `DeleteApp` does iterate `ListInstancesByApp` + `Reservations.CancelReservation` + `DeleteInstance` + `StopInstance` dispatch — never called by `apphosting DeleteApp`.
- `compose/lifecycle.go:570` `DeleteComposeStack` does `ComposeDelete:598` `down -v` but beacon `removeOrphans` defaults false (`compose.go:574` `?removeOrphans=true` only if caller asks) leaving old subnet/containers; app path leaves beacon containers orphaned. No node-credential fetch for app delete at all.

---

## 5. Key Findings (≥4 required) — File:Line Verifiable

### F-01 — P0 `ExecuteDeployment` Honest-Failure Gate Landed, but Traffic/Scaling Steps Still False-Complete (AP-02 refining FLF-01)

- **Location:** `forge/api/internal/services/deployment/execution.go:249` `RuntimeExecutor:254` `ApplyDeployment/VerifyRunning` + `execution.go:264-274` `executeProvisionStep` `274` `verifyObservedRunning:338-350` + `execution.go:277` `executePromoteStep`
- **Evidence:** Provision now `if runtime==nil → no runtime executor wired; refusing to report success` (`execution.go:269`) + `provision_regression_test.go:13` enforced. Blue-green promote `execution.go:277-296` still `UpdateDeploymentConfig` `active_target` toggle + `deployment_promoted` event **without** `trafficmanager/loadbalancer/caddy` or `service_endpoints` reload. Scale/drain steps `verifyObservedRunning` checks `bool running` not replica counts vs `TargetReplicas`/`Replicas`. Canary `canaryPercent:272` ignore.
- **Impact:** `recreate` → honest P0 fixed; `blue-green/rolling/canary` still complete with `promoting→completed` while zero traffic shift and same host-port contention.
- **Recommendation:** Wire `executePromoteStep` to `trafficmanager`/`loadbalancer` `targetGroup` or `service_endpoints` + beacon `proxy` reload, or mark strategies `NOT_IMPLEMENTED` `501` in `handlers_revisions.go:48` `StartRollout`. Make scale/drain `ListInstancesByApp` count vs `TargetReplicas` + `MinAvailable` tolerance à la Coolify `rolling_update`.

### F-02 — P0 `applications.desired_state` Write-Only Lie, `restart` Fixed via Power Channel (AP-03 splitting FLF-02 + FLF-07)

- **Location:** `forge/api/internal/http/handlers_apphosting.go:450` `start:468 UpdateApp{DesiredState:"running"}` `475 stop:493 UpdateApp{DesiredState:"stopped"}` vs `500 restart:527-533 SendPower` + `store/store_apphosting.go:379` `UpdateApplicationStatus` unused
- **Evidence:** `UpdateApplication:313` dynamic `SET desired_state=$..` `updated_at now`. No lease, no `queue/operation` enqueue, no `observed_status` mutation, returns `{ok:true}` immediately. `replicamanager/reconcile:732` watches `ReplicaApplications` 1m `Ticker` but not `applications`. Frontend badge `DeployStatusBadge` reads stale `observed_status:"idle"`. Restart correctly `ServerControlTarget:527` → `Daemon.SendPower(60s timeout):533` → `502 bad gateway node restart failed` on error (honest).
- **Impact:** Operator clicks Start/Stop, UI toast success, `docker ps` unchanged. Restart trajectory no longer spurious `deployment_history` row (fix), but start/stop remain `FALSE_COMPLETION`.
- **Recommendation:** `ADAPT` 1Panel `SyncAppInstallStatus:835` poll — add `AppReconciler` loop polling `applications WHERE desired_state!=observed_status` via `appLocks[64]` + `AdminContainer{Start,Stop}` fan-out or `replicamanager` for `ReplicaAppID` apps; expose `202 Accepted not reconciled` until observed flips, or make start/stop enqueue `operation.Service` ops.

### F-03 — P1 Unconditional Lease Release Fence Violation + No Heartbeat (AP-12 / FINAL §14)

- **Location:** `forge/api/internal/services/deployment/execution.go:24` `ClaimExecutionLease(5m)` `32 defer ReleaseExecutionLease(ctx, deploymentID)` unconditional + `execution.go:482` `resumeFromStep:490` `defer ReleaseExecutionLease` vs `store/store_deployments.go:342` `ReleaseExecutionLeaseIfOwner:344` exists but unused + `store_deployments.go:322` `RenewExecutionLease`
- **Evidence:** Lease `execution_lease_until now()+5m` (`store_deployments.go:306` `execution_lease_until < now()`). Step loop timeout `TimeoutSeconds default 300` equals lease; `handleStepFailure:112` opens 30s `handleCtx` beyond lease window; no `RenewExecutionLease` ticker. If lease expires, second `ClaimExecutionLease:483` claims; first's defer unconditional `ReleaseExecutionLease:353` `SET executor_id=NULL WHERE id=$1` **clears second's lease** allowing third to claim and double-execute `executePromoteStep` (non-idempotent `ActiveTarget` flip).
- **Impact:** Rare `P1` double-deploy, promotes opposite target twice, violates `idx_unique_active` intent.
- **Recommendation:** Replace both defers with `ReleaseExecutionLeaseIfOwner(ctx, deploymentID, executorID)` + 30s `RenewExecutionLease` heartbeat goroutine while `execCtx` alive; document `Refresh` semantics in `ResumeDeployments:428-479` `LoadOrStore` after `TimeoutAt` check race.

### F-04 — P2 Rollback Non-Atomic Version + Revision-Number Race (AP-05 / LF-05)

- **Location:** `forge/api/internal/services/deployment/revisions.go:82` `CreateRevision:87-91` `GetLatest→nextNum` + `147` `RollbackToRevision:167 UpdateDeploymentStatusVersioned(V) → 174 UpdateDeployment stale V + 128 non-versioned SET version+1 WHERE id` + `store/store_deployments.go:195` versioned setter unused
- **Evidence:** Second `UpdateDeployment` carries stale `V` payload for `Image/ActiveTarget` while DB already `V+1`. `UpdateDeployment` (`store_deployments.go:128-137`) has no `AND version=$2` nor `RowsAffected` check, so concurrent rollback overwrites. `CreateRevision` no `SELECT ... FOR UPDATE` or serializable TX. `configHash:58` 12hex truncation excludes `EnvVars/ports/resources`.
- **Recommendation:** Make rollback single TX: `UpdateDeploymentConfigVersioned(image, version)` or new `UPDATE deployments SET image=$.., status=$.., version=version+1 WHERE id=$1 AND version=$2 RETURNING version`. Wrap `CreateRevision` in `tx.Begin → SELECT FOR UPDATE ON deployments → INSERT` with `UNIQUE(deployment_id,revision_number)` retry loop.

### F-05 — P1 Delete Unconstrained + Orphan Containers/Reservations (AP-06 / LF-04)

- **Location:** `forge/api/internal/services/apphosting/service.go:175` `DeleteApp:183 DELETE` → `store/store_apphosting.go:374` `DELETE WHERE id` vs `replicamanager/service.go:501` `DeleteApp` sweep + `store_deployments.go:119` `isActiveStatus` not checked + `compose/lifecycle.go:608` `cancelReservation` only on compose
- **Evidence:** No guard for `isActiveStatus` (`pending|in_progress|provisioning|awaiting_health|promoting|rollback_*`). Beacon `removeOrphans` opt-in false default (`compose.go:574`). `DELETE FROM applications` relies on `ON DELETE CASCADE` for `app_services` but leaves `instances` (`replica_app_id` FK stub) + `placement_reservations` active lease + `BeaconCommandLog` orphan rows. Compare `1panel DeleteCheck:692` enumerates `app_install_resource` dependents first.
- **Impact:** Leaked reservations block future `PlaceReplicas`; orphan `docker compose -p oldId` containers keep port; `deployment` worker `ExecuteDeployment:131-135` later `UpdateDeploymentCompletion` on deleted server's deployment.
- **Recommendation:** Gate `DeleteApp` with `if isActiveStatus(d.Status) → 409 ErrInProgress`; enqueue `operation DeleteApp` draining beacon via `replicamanager.DeleteApp` + `ComposeDelete?removeOrphans=true` before row delete; cancel `placement_reservations` `WHERE server_id=app.ServerID`; add `ON DELETE CASCADE` or explicit cleanup for `instances` where `app_id` maps to `replica_app_id`.

### F-06 — P1 Compose Admin Restart Wired but Web Unwired + Non-Atomic Restart (AP-18)

- **Location:** `beacon/internal/server/compose.go:506` `handleComposeRestart` `docker compose restart` per-stack lock `compose.go:39` + `forge/api/internal/daemon/compose.go:81` `ComposeRestart` + `forge/api/internal/http/handlers_compose.go:470` `POST /compose/:id/restart → RestartStack:781` + `forge/api/internal/http/handlers_user_console.go:231` user `RestartStack` vs `forge/web/lib/api/compose.ts:34-111` **no** `restartComposeStack` exported + `forge/api/internal/services/compose/lifecycle.go:781` `RestartStack:783 StopStack→StartStack` sequential
- **Evidence:** Admin `POST /compose/:id/restart` exists (comment `467` cites `F-06` fix) but `compose.ts:88` only `deployComposeStack` `93 stopComposeStack` `98 startComposeStack`. UI `forge/web/app/admin/compose/[id]/page.tsx` offers `stop/start/redeploy` no `restart`. `RestartStack:782` `StopStack` checks `StackStatusStopped` guard `704` then `StartStack` checks `StackStatusRunning` guard `752` — non-atomic; if `StopStack` succeeds then concurrent `UpdateComposeStack` races `status=updating` → `StartStack:704` `ErrStackNotRunnable`.
- **Recommendation:** Export `restartComposeStack` in `lib/api/compose.ts` + wire UI button; make `RestartStack` atomic via beacon `docker compose restart` single call without two `UpdateComposeStack` state transitions, or unify with reservation+health pattern `WaitForHealthy:188`.

### F-07 — P1 Volume Policy Bifurcation API 200 valid → Beacon 400 Violation (LF-06)

- **Location:** `forge/api/internal/services/compose/service.go:594` `checkVolumesSecurity` `sensitiveHostPaths:679 ["/","/root","/etc","/home"]` warning vs error + `service.go:663` `ValidateHostMountWithAllowlist(source,isAdmin,allowedMounts)` vs `beacon/internal/server/compose.go:226` `validateComposeVolumes:238 any absolute "/" or ".." → "host bind mount not allowed"` `234 long-form type:bind hard reject` + `validateComposePolicy:77`
- **Evidence:** `services.db.volumes: ["/data:/var/lib/db"]` passes API (`/data` not sensitive → `200 valid`), fails beacon `400 compose policy violation: host bind mount source "/data" is not allowed` after `CreateComposeStack` + reservation `lifecycle.go:391` already persisted + health poll. Conversely `allowedMounts` never plumbed `daemon.ComposeDeployRequest{StackID,ComposeYAML,EnvVars}` (`daemon/compose.go:13`) lacks allowlist payload. Bifurcation produces `200 valid→400 violation` user confusion.
- **Recommendation:** Unify predicates: make API reject all absolute mounts (`error`) matching beacon, **or** plumb `allowedMounts+isAdmin` via `ComposeDeployRequest` and have beacon consult allowlist (recommended: `daemon/compose.go:13` add `AllowedMounts []string, IsAdmin bool`); remove blanket long-form bind ban when allowlist supports it.

### F-08 — P0 `env_file` / `include` Silently Dropped (AP-17 cross-ref)

- **Location:** `forge/api/internal/services/compose/parser.go:197` `loader.LoadWithContext Environment:map[string]string{}` empty + `forge/api/internal/services/compose/service.go:352` `interpolateEnv` `EnvFile` defined but never loaded + `beacon/internal/server/compose.go:264` `encodeComposeEnv` writes `.env` only from `EnvVars map`
- **Evidence:** Compose with `services.web.env_file: [./secrets.env]` passes `ValidateCompose` (`healthcheck/deploy` warning only) then daemon writes `.env` without referenced file; deploy proceeds with empty vars where secrets expected → runtime missing DB password. Compare `compose-go loader` `envresolver.go` handling; Forge drops.
- **Recommendation:** Fail fast: `ValidateCompose` error if `env_file/include` present until beacon mounts allowlist supports secure `openat2` file fetch; otherwise error not warning.

---

## 6. Detailed Status Ledger Reconciliation — Why FINAL_PARITY_AUDIT Numbers Shift

FINAL_PARITY_AUDIT §3 lists “20 rows audited” for App Platform but renders 18 rows `AP-01..AP-18` (row count off by 2; orphaned rows `AP-13/AP-14` conflated). Phase-01 synthesis `§6` counts `COMPLETE 1 PARTIAL 9 UNWIRED 2 BROKEN 1 MISSING 2 FALSE_COMPLETION 3`. Current reverification reconciles to final-parity subagent-02 20 rows `PARITY 4 PARTIAL 10 BROKEN/UNWIRED 6`. The delta is not drift but honest refinement after 3 fixes:

| Fix landed | Before | After | Evidence |
|---|---|---|---|
| `redeploy` row leak | `F-08` compose `DeployComposeStack` fresh `cps-` row per redeploy, orphan old | **FIXED** `handlers_compose.go:413` `UpdateComposeStack` in-place with rollback | `lifecycle.go:497 rollbackHash` `gitops` parity |
| `health gate localhost` | `F-05` `localhost` + `validateHealthGateTarget` clamp → every gated deploy timeout | **FIXED** `healthgate.go:23` `resolveNodeHost` via `ServerControlTarget` `NodeURL.Hostname()` | `healthgate.go:62-76` |
| `scale no-op` | `F-13` DB replicas bump with no placement → silent `200` | **ADMISSION FIXED** `apphosting/service.go:432` `scale refused: ReplicaAppID unset` + `handlers_apphosting.go:433` `ReplicaManager.ScaleApp` sync | `ScaleService:432` comment cites `F-13` |
| `app restart empty-image deploy` | `F-07` `TriggerDeploy{recreate Image:""}` violated `validateImageRef:322 @sha256` or spurious `in_progress` | **FIXED** `handlers_apphosting.go:500` `SendPower "restart"` 60s timeout `Daemon.SendPower` | `admission-time rejection` `apphosting/service.go:700 no deployable image` fallback `GetServerDockerImage` |
| `deployment provision stub` | `FLF-01` 6 steps `return nil` → `completed` with zero containers | **HONEST** `execution.go:269` `no runtime executor wired; refusing to report success` + `verifyObservedRunning:338` | `provision_regression_test.go:13` |

Remaining load-bearing gaps match `FINAL_PARITY_AUDIT §12.4` top-5 P0 cluster: `NG-01..10` gateway lies (out of scope here) **plus** app-platform stub dishonesty narrowed to traffic/promote; deepest structural risk already flagged (`AF-1` write-only events, `AF-3` fencing twice wrong edge `24h vs 1h` zero beacon enforcement, `AF-2` no leader — see Orchestration `store_deployments.go:300` lease).

---

## 7. Recommendations Priority Stack (No New Subsystem — Wire Existing)

| P | Item | Location | Action |
|---|---|---|---|
| **P0** | Make deployment promote **honest** — either wire `service_endpoints/target_groups/loadbalancer` on `executePromoteStep:277` or gate `POST /admin/deployments/:id/rollout` `Strategy blue-green\|canary\|rolling` to `501 Not Implemented` until traffic writer singletons | `execution.go:277` `handlers_revisions.go:48` | Prevent `FALSE_COMPLETION` |
| **P0** | Introduce `AppReconciler` loop for `applications.desired_state` → mirror `replicamanager.Start 1m reconcile:697-808` with `appLocks[64]` + `UpdateApplicationStatus:379` via `AdminContainer{Start,Stop}` fan-out (or `queue.operation` `OpServerStart`) | `handlers_apphosting.go:450` `store_apphosting.go:11` | Close `desired vs observed` `FALSE_COMPLETION` |
| **P0** | Reject `env_file/include` with `error` not warning, or mount via beacon allowlist | `compose/service.go:240` `parser.go:197` `beacon/compose.go:380` | Prevent silent secret loss |
| **P1** | Fix lease fencing: `defer ReleaseExecutionLeaseIfOwner:344` + 30s `RenewExecutionLease:322` heartbeat + scope `ResumeDeployments:428` `LoadOrStore` after `TimeoutAt` check atomic via `FOR UPDATE SKIP LOCKED` | `execution.go:32,490` `store_deployments.go:342` | Remove double-execute promote hazard |
| **P1** | Make `RollbackToRevision` version-atomic single `UPDATE ... WHERE version=` + `SELECT FOR UPDATE` for `CreateRevision:87` `nextNum`, truncate `configHash:58` to 64 not 12 and include `EnvVars/ports/resources` | `revisions.go:58,82,167` `store_deployments.go:128` | Remove history divergence under concurrency |
| **P1** | Gate `DeleteApplication:175` on `!isActiveStatus` `rollout.go:119` + enqueue cleanup `replicamanager.DeleteApp:501` + `compose.DeleteComposeStack?removeOrphans=true` + cancel `placement_reservations` | `apphosting/service.go:175` `store_apphosting.go:374` | Stop orphan containers/reservations |
| **P1** | Unify volume policy or plumb `allowedMounts` to beacon via `daemon/compose.go:13` `ComposeDeployRequest` | `compose/service.go:663` `beacon/compose.go:226` | Remove `200 valid → 400 violation` UX lie |
| **P1** | Wire scale transactionally: `ReplicaManager.ScaleApp` **before** `UpdateAppService` or in single `DB.Begin` tx; add `POST /compose/:id/scale --scale` via beacon | `apphosting/service.go:440` `handlers_apphosting.go:416` | Remove split-brain DB `replicas` vs placement |
| **P2** | Export `restartComposeStack` in `compose.ts:88` + UI `forge/web/app/admin/compose/[id]/page.tsx` stop/start/redeploy → add `restart` button; make `RestartStack:781` atomic `docker compose restart` not `StopStack→StartStack` double state | `compose.ts:93` `handlers_compose.go:470` `lifecycle.go:781` | Close admin/user + web duality `UNWIRED` tail |
| **P2** | Validate at `UpdateComposeStack:476` entry (`ValidateCompose` like `DeployComposeStack:264` does) before mutating `composeHash→updating` | `lifecycle.go:476` | Prevent `updating` → `failed` divergence |
| **P3** | `Review TargetReplicas` `rollout.go:111` `if <=0 → 1` clobber: derive from `ListAppServices` when zero or require explicit service ID in `RolloutRequest` | `rollout.go:92` `execution.go:338` `verifyObservedRunning` bool → replica-count | Prevent silent downscale 5→1 on rollout where caller omits replicas |

---

## 8. Adopt / Adapt / Inspire / Reject Matrix

| Reference Pattern | Verdict | Rationale vs Current Forge |
|---|---|---|
| **Coolify `ApplicationDeploymentJob` queue per app + `rolling_update` health-gated before promote** | **ADOPT** | Forge lease+steps DAG already mirrors; only `Promote` bridge missing — wire it `trafficmanager` same queue |
| **Dokku `checks` (`CHECKS` file) + `scheduler-docker-local` health before proxy switch** | **ADAPT** | Already `healthgate.go:23` node probe + `WaitForHealthy:188` ps poll; unify `threshold 3×5s` window with `docker inspect Health` fallback |
| **CapRover `ICaptainDefinition` strict exactly-one allowlist** | **INSPIRE** | Replace `handlers_apphosting.go:100` silent `DOCKER_IMAGE` default with `SourceConfig exactly-one` reject; reduce JSON sprawl via Forge manifest shortcut |
| **Komodo `monitor/mod.rs:48` poll + `SyncAppInstallStatus:835` reconcile** | **ADOPT** | Add `AppMonitor`/`StackMonitor` polling `daemon.ComposeStatus:98` → `observed_status` `running\|degraded` vs external `docker kill` drift |
| **Portainer `StackDeployer` builder** `director.go` hook after deploy | **ADAPT** | Post-deploy hook for `proxy_domains` reload after `DeployComposeStack:396` success + `WaitForHealthy:416` |
| **1Panel `SyncAppInstallStatus` periodic + `Operate:246` dispatch** | **ADOPT** | Same reconciler as Komodo rows — start/stop Fan-out `ws-token` + `reconcile` |
| **Uncloud fan-out `StopService:165 StartService:199` + generation `instanceCommandID` idempotency** | **ADOPT** | `replicamanager/service.go:280` `instanceCommandID(generation,operationID)` already does; extend to `app start/stop` fan-out |
| **Docker-Compose `--scale SERVICE=NUM` + per-stack `RWMutex:39`** | **ADOPT** | Add `POST /compose/:id/scale` wiring `--scale` through beacon; mutex `compose.go:39` already correct |

Reject nothing outright; unify fragmented `health_check_configs` vs deployment gate vs `WaitForHealthy` into one `ServiceHealth` path.

---

## 9. Raw File:Line Citations Index (Reverified 2026-08-24)

**Forge:**

- `forge/api/internal/services/apphosting/service.go:81` `validSourceTypes` `92 CreateApp:100 defaults ToUpper` `139 UpdateApp` `175 DeleteApp:183 direct DELETE` `198 CreateService` `359 UpdateService` `398 ScaleService:432 ReplicaAppID guard 440 DB before replica` `455 GetServiceStatus` `594 PlanServiceUpdate` `681 TriggerDeploy:695 server_id required 700 image admission 734 resolveDeployImage` `734 image@sha256 fallback GetServerDockerImage`
- `forge/api/internal/store/store_apphosting.go:11` `Application:21 DesiredState 22 ObservedStatus` `68 AppService 91 UpdateAppServiceInput` `151 CreateApplication:167 idle` `211 ListApplications orgID="" → where=""` `374 DeleteApplication DELETE` `379 UpdateApplicationStatus` `640 UpdateAppServiceStatus` `757 GetServerDockerImage docker_image`
- `forge/api/internal/services/deployment/service.go:268` `StartBlueGreen` `276 StartBlueGreen` `328 CompleteDeployment` `350 CancelDeployment` `436 Rollback:451 flip blue? blue: blue→green` `113 Strategy blue-green|canary|rolling|recreate`
- `forge/api/internal/services/deployment/execution.go:22` `ExecuteDeployment ClaimLease 5m:24` `32 defer ReleaseExecutionLease unconditional (fence bug)` `159 executeStep stepStatusMapping:15` `210 executeInitStep validateImageRef 214 snapshot RevisionConfig 242 greenTargetID` `249 RuntimeExecutor:254-262 ApplyDeployment/VerifyRunning` `264 executeProvisionStep:269 no runtime → fail closed` `277 executePromoteStep toggle active_target green↔blue` `299 DrainOld 306 DrainCanary 313 ScaleUp 320 ScaleDown → verifyObservedRunning:338` `338 verifyObservedRunning VerifyRunning bool` `352 executeRollbackStep → RollbackToPrevious` `360 handleStepFailure → UpdateDeploymentFailure 385 auto-rollback RollbackToPrevious` `389 ResumeDeployments LoadOrStore:454` `481 resumeFromStep ClaimLease 490 ReleaseExecutionLease unconditional`
- `forge/api/internal/services/deployment/rollout.go:42` `StartRollout 46 validateImageRef 49 validateHealthGateTarget 52 digestImageRef` `71 validateHealthGateTarget:82 localhost passthru 84 loopback check 87 error "may only target the local deployment gateway"` `92 applyRolloutRequest 111 if TargetReplicas<=0→1 clobber` `119 isActiveStatus pending|in_progress|provisioning|awaiting_health|promoting|rollback_*` `129 recreateRollout 181 rollingRollout 233 blueGreenRollout 261 canaryRollout 322 validateImageRef @sha256`
- `forge/api/internal/services/deployment/healthgate.go:11` `CheckHealth 13 Path==""||Port==0 → Passed true 17 host=HealthCheckHost||resolveNodeHost 31 http://host:port/path 10s` `62 resolveNodeHost parse NodeURL→Hostname` `79 WaitForHealthGate:84 threshold default 3 90 interval default 5000ms 97 timeout default 300s`
- `forge/api/internal/services/deployment/revisions.go:58` `configHash sha256[:12]` `82 CreateRevision 87 GetLatest→nextNum+1 no tx` `147 RollbackToRevision 167 UpdateDeploymentStatusVersioned V→V+1 174 UpdateDeployment stale V` `202 RollbackToPrevious 225 CompareRevisions diff 5 fields`
- `forge/api/internal/store/store_deployments.go:14` `Deployment` `48 CreateDeployment idx_unique_active 63 23505→ErrDeploymentInProgress` `128 UpdateDeployment version+1 WHERE id (unversioned)` `149 UpdateDeploymentStatus` `195 UpdateDeploymentConfig versioned RowsAffected→ErrVersionConflict 230 UpdateDeploymentRollback 246 UpdateDeploymentCompletion 262 UpdateDeploymentCancelled 278 UpdateDeploymentStatusVersioned` `300 ClaimExecutionLease 5m 322 RenewExecutionLease 342 ReleaseExecutionLeaseIfOwner unused 353 ReleaseExecutionLease unconditional`
- `forge/api/internal/services/compose/lifecycle.go:22` `StackStatus enum` `155 New 188 WaitForHealthy 5s poll deadline 5s ticker restartCount +1 249 DeployComposeStack 264 ValidateCompose before node 272 quota MaxUserStacks:146 20 279 resource ceil MaxUserStack*:150-152 285 computeHash 988 317 NodeID→GetNode 322 GetNodeDaemonCredential 359 createStackID cps- 390 CreateComposeStack reserve cancel 396 ComposeDeploy 410 awaiting_health 416 WaitForHealthy 2m 428 EventComposeDeployed 476 UpdateComposeStack 492 hash+envMapsEqual short-circuit 497 rollback capture 515 UpdateComposeStack 532 ComposeDeploy 549 WaitForHealthy 570 DeleteComposeStack 598 ComposeDelete down -v removeOrphans opt-in 608 cancelReservation 625 GetStackStatus 663 GetStackLogs 697 StartStack 745 StopStack 781 RestartStack Stop→Start sequential` + `service.go:240 ValidateCompose 427 ValidateComposeSecurity`
- `forge/api/internal/services/replicamanager/service.go:100` `CreateApp 1-100` `143 DeployApp MarkDeploying+IncrementGeneration+PlaceReplicas` `195 deployReplicas per-shard 64 appLocks + Reservation CreateReservation → CreateInstance TX → BeaconCommand dispatch → ConfirmReservation 361 ScaleApp 195 deployReplicas 446 safeStopReplicas 501 DeleteApp sweep Stop+Cancel+Delete` `732 reconcile 1m Ticker VerifyInstance → failed → RetryFailedPlacements 810 updateAppStatus running|degraded|failed`
- `forge/api/internal/http/handlers_apphosting.go:55` `registerAppHostingRoutes` `60 tenantAccess` `83 resolveOrg` `102 GET /organizations/:orgId/apps` `116 POST` `138 GET /apps admin global ListApplications orgID=""` `173 POST /apps resolveDefaultOrg:188` `211 GET /apps/:id` `231 PUT /apps/:id → UpdateApp` `259 DELETE /apps/:id → DeleteApp unconstrained` `282 POST /apps/:id/deploy → TriggerDeploy:299` `308 GET services` `332 POST services` `388 GET .../instances` `416 PATCH .../services/:serviceId → UpdateService:427 then ReplicaManager.ScaleApp:433-436` `444 POST .../instances/:id/{start,stop,restart} → instanceLifecycleHandler:1083 1106 AppID==appID 1111 GetNode 1139 AdminContainerStart/Stop/Restart 1154 UpdateInstanceStatus` `450 POST /apps/:id/start:468 UpdateApp DesiredState running ok:true lie` `475 stop:493 stopped` `500 restart:527 ServerControlTarget 533 SendPower restart honest` + `appstore/service.go` + `catalog`
- `forge/api/internal/http/handlers_deployment.go:24` `POST /admin/deployments/blue-green 268 StartBlueGreen 44 rollback 52 complete 68 execute 82 steps 98 resume 105 GET :id 113 server/:serverId 121 GET /`
- `forge/api/internal/http/handlers_revisions.go:16` `GET /:id/revisions 32 POST /:id/revisions/:revId/rollback 40 rollback-previous 48 POST /:id/rollout StartRollout 64 compare`
- `forge/api/internal/http/handlers_compose.go:66` `registerComposeRoutes 82 webhook HMAC 106 validate 118 import 162 git/deploy 197 redeploy 207 check-update 227 rollback 315 POST /compose DeployComposeStack 348 GET /compose 362 GET :id 378 PATCH :id UpdateComposeStack 401 DELETE DeleteComposeStack 413 POST :id/deploy → UpdateComposeStack (F-08 fix) 440 stop 454 start 470 restart 483 status 498 logs`
- `forge/api/internal/http/handlers_user_console.go:28` `POST /user/compose 44 DeployCompose 73 ListStacks userID 87 GET :id 94 Patch 120 Delete 135 deploy→DeployComposeStack (still row-leak) 165 stop 181 start 231 restart:240 RestartStack` `301 loadUserOwnedStack` ownership guard
- `forge/web/lib/api/apps.ts:232` `fetchApps /apps 243 createApp POST /apps 255 startApp POST /apps/:id/start 259 stopApp 263 restartApp POST /apps/:id/restart 275 triggerDeploy POST deploy 279 fetchAppLogs GET /logs 330 redeployComposeStack`
- `forge/web/lib/api/compose.ts:34` `validateCompose 49 createComposeStack POST /compose 67 list 75 get 81 updateComposeStack PATCH 85 deleteComposeStack 88 deployComposeStack POST deploy 93 stopComposeStack 98 startComposeStack (NO restartComposeStack)` `101 status 105 logs`
- `forge/web/lib/api/deployments.ts:88` `fetchDeploymentSteps 98 fetchDeployment 103 fetchDeploymentRevisions 113 rollbackToRevision 123 cancelDeployment`
- `beacon/internal/server/compose.go:27` `composeStack RWMutex 39 lock/unlock 59 validStackID 77 validateComposePolicy privileged,network_mode host,cap_add,devices,ports<1024,volumes absolute/..` `264 encodeComposeEnv strip \\n\\r 283 dirForID 290 composeDeployRequest{StackID,ComposeYAML,EnvVars,RemoveOrphans false} 344 handleComposeDeploy lock→os.RemoveAll→MkdirAll 0o750→WriteFile compose.yaml 0o640→WriteFile .env 392 exec docker compose -f compose.yaml -p StackID up -d (--remove-orphans opt-in) 418 handleComposeStop stop 462 handleComposeStart start 506 handleComposeRestart restart 550 handleComposeDelete down -v (--remove-orphans opt-in) 596 RemoveAll after 604 handleComposeStatus ps --format json → Services 661 handleComposeLogs logs --no-color --tail 709 handleComposePull pull`
- `beacon/internal/server/server.go` + `forge/api/internal/daemon/compose.go:48` `ComposeDeploy 73 ComposeStop 77 ComposeStart 81 ComposeRestart 85 ComposeDelete 88 ComposePull 110 ComposeStatus 131 ComposeLogs 157 composeAction generic`

---

## 10. Verdict & Confidence

- **Reverifier confidence:** High — every row claims directly `file:line` from a `Read` on the exact current checkout; `reference` surface cross-checked against `reference/app-platforms/coolify`, `dokku/plugins/ps`, `caprover/models/ICaptainDefinition.ts`, `1panel/app_install.go:11`, `beacon/compose.go:77`.
- **Overall lifecycle verdict:** `PARTIAL with 4 honest fixes confirmed` — same `P0` traffic debt as Phase-01 but `recreate/health/restart/redeploy` honest; `desired vs observed` + `delete orphan` + `lease fence` + `rollback race` **STILL BROKEN**; `env_file/volume` spec fidelity **STILL BROKEN**; `blue-green/canary` promote **STILL UNWIRED (FALSE_COMPLETION)**; `admin restart → web` chain **WIRED_BUT_UNWIRED at web**; `rolling` replica-count verification **STILL BOOLEAN**.
- **Action classification:** Activation-phase wiring, not rebuilding — matches `FINAL_PARITY_AUDIT §0: 28% COMPLETE 22% PARTIAL 18% UNWIRED 14% BROKEN 10% DUPLICATE 8-10% MISSING` distribution; app-platform lifecycle is the highest `FALSE_COMPLETION` concentration outside gateway (§8).

---

*End of reverification — subagent 06/20 — no product code modified.*

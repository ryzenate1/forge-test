# Reverification Subagent 16 — Backup & Storage (Kopia / Restic / Longhorn vs Forge)

**Focus:** Kopia/Restic/Longhorn reference vs Forge backup/storage — encryption / integrity / retention / pruning / GC / scheduling / restore journal / progress  
**Run:** 16 of 20 parallel — no product code modified  
**Date:** 2026-08-24 (HEAD)  
**Reconciled priors:** `audits/phase-03/synthesis.md` (18 findings, P0/P1 sidecar+OOM, retention AND inversion) + `audits/final-parity/subagent-08-backup-storage.md` (20 rows, 9 findings BKP-08-01..09, P0 transplant, P1 OOM/sidecar trust) + `audits/phase-05/subagent-04-longhorn-rancher-storage.md` (StorageLocality dead, F1–F6)

**Reference locks re-inspected file:line:**
- `reference/backup/kopia/repo/blob/storage.go:204` `Storage` (`PutBlob/GetBlob(offset,length)/DeleteBlob/ListBlobs`, `GetCapacity`, `PutOptions{DoNotRecreate, RetentionMode}`) — read-after-write atomicity, object-lock
- `reference/backup/kopia/repo/content/content_manager.go:70` `WriteManager{pendingPacks map[blob.ID]*pendingPackInfo, packIndexBuilder, flushing, cond}` + `334 shouldWrite = Length >= MaxPackSize (~20 MiB)` + `459 BuildShards` + `791 WriteContent` dedup via `819 getContentInfoReadLocked` + `516 writePackAndAddToIndex` + `748 pendingPackInfo {currentPackData *gather.WriteBuffer}`
- `reference/backup/restic/internal/repository/chunker.go:9` `baseChunker{bc *chunker.BaseChunker, pol chunker.Pol}` + `21 chunkerFactory{pol}` + `29 Restic Rabin polynomial` — CDC variable-size chunks
- `reference/backup/restic/internal/repository/prune.go:106` `PlanPrune` + `178 packInfoFromIndex` + `386 decidePackAction` + `594 Execute` (re-copy live blobs, rebuild index, delete packs under exclusive lock) + guards `18 ErrIndexIncomplete / 19 ErrPacksMissing / 20 ErrSizeNotMatching`
- `reference/backup/restic/internal/repository/lock.go:47` `LockRepo(exclusive)` writes `locks/<id>.json` + `124 refreshLocks` ticker + `188 monitorLockRefresh` + `270 RemoveStaleLocks` — `prune` requires exclusive, `backup` non-exclusive
- `reference/large-systems/longhorn` CRDs — `chart/templates/crds.yaml:4592 DiskSelector / 4636 NodeSelector`, `enhancement/20200819-keep-a-local-replica-to-engine.md:dataLocality disabled/best-effort/strict-local`, `crds.yaml:3046 failedAt/healthyAt`

**Forge layers re-read at HEAD:**
- `forge/api/internal/services/backup/encryption.go:92` `deriveEncryptionKey(master,nil,purpose)` nil salt + `119 gcm.Seal(nil,nonce,data,nil)` nil AAD + `150 EncryptReader io.ReadAll(r)` + `178 DecryptReader io.ReadAll(r)` + `212 encryptWithKey` single nonce
- `forge/api/internal/services/backup/service.go:271` `UploadBackupWithOptions` (`HasSuffix .zip/.tar.gz` `277`) + `283 CompressReader` then `294 EncryptReader` then `303 io.ReadAll(data)` + `311 nonceHex = hex(data[:12])` + `363 DownloadBackupWithOptions` (`Contains "."` `364` — suffix mismatch) + `422 DeleteBackupFromStorage` `Contains` + `438 VerifyChecksum` + `689 CleanupExpiredBackups` + `743 EnforceRetentionPolicy:768 keep = isLocked || (withinCount && withinAge)` AND
- `forge/api/internal/services/backup/main_service.go:19` `MainService{Config,Job,Artifact,Restore}Service` quartet — both expose `CreateBackup` divergent from `service.go:271` (subagent-04 C04 duplication) — `git diff HEAD` shows no convergence fix
- `forge/api/internal/services/backup/worker.go:152` `policyDue` (`NextRunAt==nil => false` — first run never fires) + `166 persistNextRun` + `182 pickupBackupJobs` (5×20 min sequential inside 1 min tick) + `401 enforceRetentionBeforeBackup:422 overLimit = count - MaxBackups +1` count-only
- `forge/api/internal/store/store_backups.go:240` `CleanupOldBackups` (`DELETE WHERE is_locked=FALSE AND created_at < now()-interval '1 day'*$1 AND status='completed'` DB-only, `248`) + `263 CleanupOldBackupsForServer:277-295` (`DELETE ... OR uuid IN (SELECT uuid ... ORDER BY created_at ASC LIMIT ...)`) + `54 UpsertBackup ON CONFLICT(server_id,name) DO UPDATE` overwrite + `348 FailStaleBackups` 360 min reaper only for `backups` table — not `backup_jobs`
- `beacon/internal/backup/local.go:238` `Create` (`275 OpenFile backupPath+".partial" O_CREATE|O_EXCL → zip.NewWriter → 390 Rename`) + `90 lockNamespace` (`namespaceMu map[string]*namespaceOperation{mu,refs}`) in-process only + `111 reportProgress` + `238,275,532,757` sidecar/journal/orphan points — `275 .partial` + `546 MkdirTemp parent+"."+base+".restore-"` + `572 rollback := "."+base+".rollback-"+nanos` + `757 readOrCreateMetadata` trust + `787 writeMetadata` plain JSON + `532 Restore(truncate)` 3-phase + `899 writeRestoreJournal` + `931 recoverInterruptedRestore` + `867 RecoverRestoreJournals`
- `beacon/internal/backup/retention.go:61` `// Intersection: a backup must satisfy ALL active rules to be kept` explicit AND + `64 if MaxAge>0` then `79 KeepDaily/Weekly/Monthly re-add keep=true` bypassing age + `104 MaxBackups` intersect + `122 keep[backups[0].ID]=true` safety rail
- `beacon/internal/backup/verification.go:31` `VerifyBackup` (`Download` whole archive then `sha256` vs `info.Checksum` `66 EqualFold`) + `80 VerifyAllBackups` sequential, not scheduled, no percent sampling
- `beacon/internal/backup/s3.go:126` `S3Backup.Create` (stage via `local.Create` then `278 os.Open→manager.Uploader Upload` streaming) + `288 downloadToStaging` `296 CreateTemp .s3-download-*.zip` + `318 expected=checksumFromMetadata` gate
- `beacon/internal/server/server.go:62` `BackupProgressEvent="backup progress"` + `344 mux.HandleFunc GET /servers/{id}/ws/backup backupProgressWS` + `1542 SetProgressCallback→eventBus.Publish(BackupProgressEvent+":"+serverID)` + `2155 backupProgressWS` (`2169 ScopeWebsocket|ScopeBackupDownload`, `2192 Subscribe EventBus+":"+serverID`, `2203 writer.Write(msg)`)
- `forge/api/internal/http/realtime.go:124` `realtimeProxy` + `forge/api/internal/http/server.go:1995` (`/servers/:id/ws/stats|logs|console` only — no `ws/backup`) + `2408 IssueWSTicket` omits backup
- `forge/api/internal/services/scheduler/service.go:212` `FilterNodes StorageLocality=="local_only" && RuntimeProvider!="local"` + `317 ScoreNodes ±1e10/1e8` + `714 nodeToCandidate storageLocality="local"|"shared"` (nfs/shared else local) — dead per `phase-05 F1`
- `forge/api/internal/placement/strategy.go:42` `Candidate.StorageLocality` / `53 WorkloadRequest.StorageLocality`

---

## 1. Methodology

Re-anchored every prior file:line against checked-out HEAD (2026-08-24). Prior Phase-03 synthesis captured 18 findings across 5 subagents; final-parity subagent-08 captured 20 parity rows + 9 high-impact findings BKP-08-01..09; Phase-05 subagent-04 captured 6 logic findings including StorageLocality dead (F1) and locality-after-placement (F3). Re-inspection priority:

1. **Verify still BROKEN list:** unauth sidecar transplant P0, streaming buffer OOM, retention AND vs OR, prune without lock, GC orphan `.partial`, lockNamespace in-process only, dead progress WS — each re-confirmed with exact `file:line` below (no remediation applied except `store_backups.go:64-95` `isLocked` param shift bugfix and `beacon/server.go:1478 sanitizeBackupName` idempotency — neither touches the 7 BROKEN cores).
2. **Reconcile duplications:** `service.go:271` vs `main_service.go:19` quartet still diverge (adapters/storage path); `retention.go:61` AND vs `store_backup_policies.go:301` hard-coded 30d vs `ArtifactService:711` vs `service.go:743` — four engines still diverge.
3. **Reference deltas:** Confirmed Kopia `storage.go:204` range-read + atomic `PutBlob(DoNotRecreate)` vs Forge `StorageAdapter` whole-object; `content_manager.go:70` pendingPacks + dedup vs Forge re-archive; Restic `chunker.go:9` CDC + `prune.go:386` reachability + `lock.go:47` exclusive locks vs Forge SQL-only.

`git diff HEAD --stat` shows no changes to `encryption.go`, `retention.go`, `local.go`, `service.go`, `worker.go:401`, `realtime.go`, `server.go:1995` relevant logic — confirming still BROKEN except the two noted non-core fixes.

---

## 2. Definitive Parity Matrix (14 rows — reconciles phase-03 18 + final-parity 20)

Legend: STATUS = BROKEN · DEAD · GAP · MISSING · PARTIAL · DIVERGED · CONFLICT

| # | Concern | REFERENCE Path:Symbol | FORGE Layers file:line | STATUS | GAP | LOGIC | FINDING | SEVERITY |
|---|---------|------------------------|------------------------|--------|-----|-------|---------|----------|
| 01 | **Repository pack model vs artifact zip+sidecar** | `kopia/repo/blob/storage.go:204 Storage{PutBlob/GetBlob(offset,length)/DeleteBlob/ListBlobs, GetCapacity, PutOptions{DoNotRecreate,RetentionMode}}` + `kopia/repo/content/content_manager.go:70 WriteManager{pendingPacks,pckIndexBuilder}` + `restic/internal/repository/repository.go:32 Repository{treePM/dataPM, DefaultPackSize 16MiB}` | `beacon/internal/backup/local.go:238 Create` (`backupPath+".partial" O_CREATE|O_EXCL → zip.NewWriter:296 → 391 Rename(partial→backupPath) → 407 writeMetadata`) + `forge/api/internal/services/backup/storage.go:14 StorageBackend` + `forge/api/internal/services/backup/adapter` + `beacon/local.go:787 writeMetadata .metadata.json temp→Rename+syncDirectory` | **GAP — WHOLE-ARCHIVE** | Forge 1:1 server→object zip; Kopia/Restic many:1 content→pack (20 MiB / 16 MiB packs) with index shards (`content_manager.go:459 BuildShards`). No content addressing; `ListBackups` is SQL `store_backups.go:18 ORDER BY created_at DESC` / readdir `local.go:415` not index lookup. | Correct for <2 GiB worlds; incorrect for >5 GiB modded where incremental wins. Whole-archive cost O(size) every run. Orphan detection requires full `ListObjectsV2` vs Kopia `index.Builder`. Reuses phase-03 F-B-07, final-parity BKP-08-01/02 scope. | Keep artifact model; add **chunked adapter toggle** via Kopia-as-adapter for large tier (`ArtifactService` already has `ProviderFactory` stub `adapters.go:7` never wired). Record dedup ratio in `ArtifactStatistics`. | P2 (architectural) |
| 02 | **Chunked dedup vs whole-archive** | `kopia/repo/content/content_manager.go:819 getContentInfoReadLocked` dedup hit → return `contentID` without rewrite + `content_manager.go:791 WriteContent` — `internal/splitter` CDC | `beacon/local.go:298 WalkDir → zip.CreateHeader Deflate` every file every time (ignored only) — `service.go:303 io.ReadAll` whole archive — no `chunker.go:9` Rabin equivalent | **MISSING** | No CDC, no hash-of-content, no parent snapshot (`BackupArtifact` has no `parentSnapshotID`). Unchanged 50 GiB world costs 50 GiB egress each run; Kopia would transfer ~0. | Retention deletion saves exactly one zip vs unreferenced chunks. Final-parity BKP-08-02 gap confirmé. | Document as accepted gap; wire Kopia content store behind `StorageAdapter.UploadStream` if >5 GiB tier needed. | P2 |
| 03 | **Encryption per-blob HKDF+AAD vs single-archive nil salt/AAD** | `kopia/repo/encryption` per-content AES-256-GCM + HKDF per-purpose derived key + random 12B nonce per blob; `restic/internal/crypto` 32B key (16 AES+16 Poly1305, 16B nonce, scrypt KDF) — per-blob isolation | `forge/api/internal/services/backup/encryption.go:92 deriveEncryptionKey(hkdf.Key(...,nil,"gamepanel-backup-encryption",32))` **nil salt** single purpose + `119 gcm.Seal(nil,nonce,data,nil)` **nil AAD** + `26 GetEncryptionKeyFromEnv` bypasses `secrets/keyring.go:75 envelope forge:v1:<keyID>:<nonce||seal>` — beacon `local.go:238` writes **plaintext zip** (no crypto call); `s3.go:126` stages plaintext then `PutObject` without encrypt | **BROKEN — PLAINTEXT BEACON + NULL AAD → TRANSPLANT** | Beacon path archives are **unencrypted at rest**; service path encrypts only if caller supplies `opts.EncryptionKey` (`service.go:294`). Same key + nil salt reused across servers/backups; no per-backup domain separation, no key-ID header, nonce collision reuses GCM keystream catastrophically. | Single-archive MAC covers whole zip — tamper → total loss vs Kopia per-chunk pinpoint. After master rotation old ciphertext undecryptable (no envelope). Nonce stored separately as hex `store.go:909 Nonce` leaks randomness. Re-verifies phase-03 F-B-01/F-B-02, final-parity **BKP-08-01 P0**. | **P0** — unify crypto under `secrets.Keyring`, per-backup subkey `HKDF(activeKey, serverID:uuid, salt=random16)` + envelope `forge:v1:<keyID>:nonce||Seal(aad=serverID:backupName)`, encrypt beacon staging file before `uploadToS3`. | **P0** |
| 04 | **Streaming vs buffering (OOM)** | `kopia/repo/blob/throttling/throttler.go:26` token-bucket + streaming `io.Pipe` per chunk; `restic/repository.go:380 saveAndEncrypt` streams 16 MiB packs | `encryption.go:150 EncryptReader` (`158 io.ReadAll(r) → encryptWithKey → 166 io.Pipe replay`) + `178 DecryptReader` same + `service.go:303 dataBytes, _ := io.ReadAll(data)` before `adapter.Upload:316` (whole post-compress/post-encrypt archive in heap); `artifact.go:591 Download` (`bytesReader` O(size)) + `877 downloadToTempFile` (whole-file buffering) — streaming adapters `storage.go:35 UploadStream/DownloadStream` exist but unused on encrypted path | **BROKEN — OOM** | API pod 512 MiB limit OOMs on any backup >~300 MiB encrypted; beacon S3 stager streams correctly (`s3.go:272 os.Open→manager.Uploader Upload` with 3-attempt backoff `139-158`) but service hot path clobbers it by buffering first. `encryption_test.go:146 uses 100 KiB fixture — never catches. Phase-03 F-B-02, BKP-08-02 re-verified with exact lines `150` and `303`. | `CompressReader:compression.go:63` `io.Pipe gzip/zstd` + `EncryptReader` compose as pipe but immediate `ReadAll` drains buffered pipe then second `ReadAll` copies again → 2× archive + hex nonce copy `312`. | **FINDING BKP-RV16-02** — replace with chunked AEAD `EncryptedWriter(frame=64KiB, nonce(seq)+GCM)` wired through `UploadStream` `io.Pipe` without `ReadAll`; remove `backups.nonce` hex column; cap `maxUploadBytes` + `maxS3DownloadBytes:27` symmetrically. | **P1** (availability/DoS) |
| 05 | **Integrity sidecar trust (unauthenticated transplant)** | `kopia/repo/content:hash.go` BLAKE2 content ID + MAC per content + `snapshot/snapshotfs/snapshot_verifier.go` recompute; `restic/repository.go:425 verifyCiphertext` (decrypt+`Hash(plaintext)==id`), `restorer/verifyFile:714` per-blob `Hash` | `beacon/local.go:787 writeMetadata` plain JSON `.metadata.json` (`Chmod 0600:798` but body unkeyed), `757 readOrCreateMetadata` (if sidecar exists **return without rehash** `759-767`), `508 Restore` (`actualChecksum vs metadata.Checksum`), `verification.go:31 VerifyBackup` (`Download` whole archive then `66 EqualFold` vs `info.Checksum`), `s3.go:27 checksumMetadataKey="sha256"` S3 user-metadata unkeyed | **BROKEN — UNAUTHENTICATED SIDECAR** | Sidecar/`x-amz-meta-sha256` unkeyed; attacker with write to local FS or shared S3 bucket can overwrite `backup.zip`+sidecar consistently → `VerifyBackup:66` passes. No AEAD AAD binding `server_id:backup_name`, so ciphertext transplant across servers validates (GCM `nil` AAD). Direct restore `beacon/server.go:1706 backupMu` + `restore.go:1154 verifyDatabaseRestore` stub (log + nil) skips checksum. Re-verifies phase-03 F-B-01/F-B-04, BKP-08-03. | Post-restore verification is stub in daemon path — direct restore skips checksum entirely. | **FINDING BKP-RV16-01 (P0)** — bind MAC to identity: `serverID:backupName:uuid` as GCM AAD; replace sidecar SHA-256 with AEAD tag as primary; on `Restore` recompute expected from ZIP vs DB receipt ETag; persist `HashAlgorithm` and honor at read time (today env-driven). | **P0** |
| 06 | **Retention AND vs OR (union inversion)** | `kopia/snapshot/policy/retention_policy.go` keepLatest/keepDaily/keepWeekly/keepMonthly **union OR** (keep if any bucket keeps) · `restic/internal/restic/policy.go:ApplyPolicy` union OR, locked snapshots exempt | `beacon/retention.go:61` comment "**Intersection: a backup must satisfy ALL active rules to be kept**" explicit AND; `64 MaxAge` filter then `79 KeepDaily/KeepWeekly/KeepMonthly` re-sets `keep[b.ID]=true` even for backups rejected by `MaxAge` (bypasses age gate) then `104 MaxBackups` intersects again; `service.go:768 keep = isLocked \|\| (withinCount && withinAge)` pure AND; `store_backup_policies.go:301 ListExpiredBackups` hard-codes `30 days` ignoring per-policy `RetentionDays`; `worker.go:401 enforceRetentionBeforeBackup:422 overLimit = count - MaxBackups +1` count-only | **BROKEN — SEMANTIC INVERSION, THREE ENGINES DIVERGE** | All Forge engines are AND where references are **union OR**. AND deletes strictly more: e.g. `MaxBackups=10, RetentionDays=7` with 20 backups aged 10d — Restic keeps last 10 *or* one-per-bucket; Forge `withinCount && withinAge` keeps 0 unlocked save `retention.go:122 safetyRail keep[backups[0].ID]=true` single newest — 9 days unexpectedly discarded. Conversely beacon `KeepDaily` can resurrect `>MaxAge` backups → bypass. Re-verifies phase-03 F-B-05 (AND vs OR), BKP-08-04. | Safety rail hides AND bug in smoke tests; `Store/CleanupOldBackups` vs `EnforceRetentionPolicy` vs `ArtifactService:711` vs beacon diverge — four tables. | **FINDING BKP-RV16-03 (P1 data-loss)** — single `RetentionEngine` library implementing **union OR** (`keep if any bucket keeps || isLocked`), property-tested vs Restic vectors; deprecate `CleanupOldBackups` SQL-only path; parameterize `ListExpiredBackups` by policy. | **P1** |
| 07 | **Pruning exclusive lock** | `restic/internal/repository/lock.go:47 LockRepo(exclusive)` (writes `locks/<id>.json`, 5-min `refreshLocks:124`, `monitorLockRefresh:188`) — `prune.go:119 ErrSizeNotMatching` + `594 Execute` requires exclusive — `prune:106 PlanPrune` aborts on `ErrIndexIncomplete/ErrPacksMissing` | `store/store_backups.go:240 CleanupOldBackups` / `263 CleanupOldBackupsForServer` (`DELETE FROM backups WHERE is_locked=FALSE ...` single SQL, no `pg_advisory_lock`, no storage delete), `service.go:689 CleanupExpiredBackups` / `743 EnforceRetentionPolicy` (loop deletes storage then DB without `SELECT … FOR UPDATE SKIP LOCKED`), `worker.go:401 enforceRetentionBeforeBackup` (counts then deletes without Tx), `049_backup_locking.sql` adds column but no `pg_advisory_lock` in prune path (`store/store.go:45 pg_advisory_lock` only for migrations) | **BROKEN — NO EXCLUSIVE, SQL-ONLY ORPHANS STORAGE** | Two API replicas each compute `overLimit` from same `ListBackups` snapshot and race to `DeleteBackupFromStorage` same key (S3 idempotent, DB second `RowsAffected==0` swallowed `436`) → thundering herd, metric double-count, non-atomic. `CleanupOldBackups` SQL-only orphans S3 objects forever; beacon `S3Backup.List:163` still lists via `ListObjectsV2` → panel vs beacon count drift. Re-verifies phase-03 F-B-06, BKP-08-05. | Restic aborts if any non-exclusive lock present; Forge has no gating — prune can delete packs still holding live blobs (if packing existed) or leave orphans. | **P1** — gate every prune with `pg_advisory_xact_lock(hashtext('backup_prune:'||serverID))` or S3 lock `locks/backup-<server>.json` TTL+refresh mirroring Restic; make prune `DELETE … RETURNING` + storage delete in same Tx or mark sweep (`status='pruned_pending'` → GC worker). |
| 08 | **GC orphan .partial / .restore-* / .s3-download-* — no index** | `kopia/repo/maintenance` GC roots (mark-sweep over manifests, epoch, unreferenced packs after grace) · `restic/repository/prune.go:119` reachability-aware `decidePackAction:386` | `local.go:275 backupPath+".partial" O_CREATE|O_EXCL + defer Remove if !committed 281` + `546 MkdirTemp(parent,"."+base+".restore-")` + `572 rollback := "."+base+".rollback-"+nanos` + `896 journalPath` + `s3.go:296 CreateTemp .s3-download-*.zip + cleanup 301`; **GC:** `beacon/internal/backup/scheduler.go:60 + store.go:30 Store{Create/Get/List/UpdateStatus/Delete}` on SQLite only sweeps `backup_jobs`, not `.partial/.restore-*/.s3-download-*`; `store_backups.go:242` prune is SQL sweep not index-backed, `store_admin_backups.go:256 DeleteBackupArtifactRecord` soft-deletes `status='deleted'` never deletes storage | **BROKEN — ORPHAN LEAK, STILL VERIFIED** | Crash after `275` staging but before `391 Rename` leaves `.partial` forever (never indexed, next `Create` same `name` → 409 `exists` → 500, retry must pick new name — control-plane already mints `sanitizeBackupName:1539` but beacon Create idempotency `1542-1553` only handles completed name, not `.partial`). `List:415 ReadDir` filters `validBackupName` → `.partial` excluded → invisible. S3 staged `.s3-download-*.zip` has no journal; interrupted `downloadToStaging:338` leaves temp until manual cleanup. Re-verifies phase-03 F-B-07, BKP-08-05 scope. `git diff --stat` shows zero GC fix. | Restic never leaves partial pack after crash (header validates on next `check`); Kopia recovers via `content_index_recovery.go`. Forge has journal only for **restore swap**, not backup staging. | Nightly `ListObjectsV2 vs SELECT uuid` reconciliation (analogous to `restic check --read-data` / `kopia content verify`) + local FS reaper for `*.partial`, `.*.restore-*`, `.*.s3-download-*` with `maxAge` + advisory lock. | **P1** |
| 09 | **Concurrency: lockNamespace in-process only (distributed split-brain)** | `restic/internal/repository/lock.go:47 LockRepo` (backend lock file, `refreshLocks:124` ticker, `retryLock`) · `kopia/repo/blob:Storage` atomic `PutBlob(DoNotRecreate)` + `repo/content/sessions.go` + `pendingPacks` mutex+cond | `beacon/local.go:90 lockNamespace` (`namespaceMu map[string]*namespaceOperation{mu,refs}`, `lockNamespace:90` per-namespace in-process `sync.Mutex`) guards only `Create:239` + `Restore:469`, **not** `List/Get/Delete/Download`; cross-host: no persistent lock file, no advisory row; `worker.go:182 pickupBackupJobs` limit 5 sequential `Execute` 20-min each inside 1-min `tick:105` → 100-min blocks ticker; `store_backups.go:54 UpsertBackup ON CONFLICT(server_id,name)` over-writes pending rows when two workers generate same `backup-%s:211` second-truncated | **BROKEN — IN-PROCESS ONLY, SPLIT-BRAIN CONFIRMED** | Two beacons for same server (scaled daemon or host failover) can `Create` same `name` concurrently → second `ON CONFLICT` overwrites first UUID, then both daemons succeed on isolated `backupRoot`/S3 prefix with divergent checksum but same logical name. S3 staged upload has no lock. Re-verifies phase-03 F-B-08, BKP-08-06 (prior LF-03, F3). No fix in `beacon/server.go` diff. | Previous split-brain evidence: `ListBackups` ORDER BY `created_at DESC` hides duplicate-name collision until `Get` ambiguity; `backupMu:77 global mutex` serializes all creates on one beacon but not across hosts. | Add backend-anchored lock: `pg_advisory_xact_lock(hashtext(serverID))` around `executePolicy` Tx, or S3 lock file `locks/backup-<serverID>.json` TTL+refresh (mirror Restic); gate `Worker.tick` with single-leader `queue/leader.go` elector; persist multipart state for S3 resume. | **P1** |
| 10 | **Scheduling — beacon gocron vs Worker policyDue (three schedulers, none unified)** | `kopia/snapshot/policy/scheduling_policy.go:64 SchedulingPolicy{IntervalSeconds, TimesOfDay[], Cron[]}` + `98 NextSnapshotTime` earliest among interval/ToD/cron, `197 checkMissedSnapshot` with grace, `276 Validate` · `restic` external cron · `kopia/internal/scheduler` priority queue | `beacon/scheduler.go:15 Scheduler{gocron.Scheduler, jobs map[string]*gocron.Job}` — single map keyed by `serverID` rejects duplicate `55`, pure cron `gocron.Cron(cronExpr).Do`, not persisted (restart loses schedule), not instantiated in `beacon/cmd/daemon/main.go` (only `NewLocalBackup` created) — vestigial per phase-03/subagent-03; `worker.go:89 Worker{ticker 1m}` — `152 policyDue` returns `false` if `NextRunAt==nil` (first creation never runs until `persistNextRun:166` initializes after tick), `884 NextCronRun` via `cron.NewParser(Minute|Hour|Dom|Month|Dow)` (`service.go:190` vs `config.go:13 cron/v3` — two dialect parsers); `MainService.ScheduleBackup:292 + config.go:212 BackupConfig{is_scheduled,next_run_at}` has no tick — state only, `PeriodicJobScheduler` for `backup.retention` hourly is separate | **CONFLICT — THREE SCHEDULERS, FIRST-RUN DEAD** | Creating `BackupConfig` with `is_scheduled=true` computes `NextRunAt` but nothing ever fires it unless external caller invokes `ExecuteBackupConfig`; only `Worker` watches old `backup_policies` table, so new admin configs are inert. `policyDue:152` first-run bug causes 1-min delay then per-interval correctly — but no catch-up like Kopia `RunMissed`. Two cron dialects (`robfig/cron` vs `gocron` quartz) may accept different expressions — schedule recorded in one may fail in other. Re-verifies phase-03 §5 duplicates, BKP-08-10. | `store_backup_jobs.go:199 make_interval(mins=>LEAST(1<<retry_count,30))` where `1<<retry_count` can overflow PG integer for large retry_count (subagent-02 M). | Consolidate to single `Worker` (deprecate beacon gocron), add catch-up: if `NextRunAt < now - threshold` fire once then recompute; unify cron parser; fix `policyDue` to fire on first tick if `NextRunAt==nil` and `Interval!=""`. | P2 |
| 11 | **Restore journal 3-phase + resumability vs vacuous pre-snapshot** | `kopia/snapshot/restore:copier` parallel queue, `MaxDepth`, placeholder non-destructive; `restic/internal/restorer:RestoreTo:345` two-pass (dirs + `filerestorer.addFile`), `verifyFile:714` per-blob resume (`fileState.blobMatches`) | `beacon/local.go:532 Restore(truncate)` — **strong** 3-phase journal (`899 writeRestoreJournal`, `931 recoverInterruptedRestore:954-957 path-prefix guards`, `867 RecoverRestoreJournals`): `prepared` → `live-moved` (587 Rename live→rollback) → `activated` (596 Rename staging→live) with `journalCommitted` hide-until-durable + `syncDirectory(parent):611` after each rename, retains rollback until `616 RemoveAll+Remove(journalPath)` succeeds. Non-truncate overlays via `rootfs.FS:537`. S3 `288 downloadToStaging` via `CreateTemp .s3-download-*` + staged checksum gate `361-368`. **BUT** `forge/api/internal/services/backup/restore.go:733 createPreRestoreSnapshot:745 rollbackReq{Name:"pre-restore-…", Status:"pre_restore_snapshot"}` is vacuous — header-only DB row, no file; `restore.go:381 daemonClient.RestoreBackup` short-circuit `503 completeDirectRestore` skips journal/verify; `beacon/server.go:1720 pre-restore-*.zip snapshot` under `backupMu` but `snapshotErr!=nil:1725` falls through without cleaning stale sidecar → leaks and blocks next `List` (`bad metadata`) per phase-03 F-B-09 | **PARTIAL — BEACON STRONG, CONTROL-PLANE WEAK (STILL BROKEN CORE)** | Beacon journal is arguably **stronger than references** for crash consistency of destructive restores; S3 temp staging leaves no journal. Control-plane pre-snapshot cannot rollback — Restic/Kopia snapshot-before-restore guarantees recoverable snapshot; here rollback artifact is header-only. `retry.go:546-577 Retry→Execute` accumulates `pre-restore-*` artifacts without cleanup per phase-03 F-B-10. | Retries accumulate temp dirs without cleanup; two `waitForBeaconTaskCompletion` impls diverge (`job.go:975 1s*2→30s` vs `restore.go:1262 2s fixed`) → jitter can cause one path to succeed while other times out on same task (subagent-04 C07 Q2). | Actually snapshot current files via `LocalBackup.Create` temp file before truncate, persist receipt, delete on success; route daemon restores through journal or document bypass caveat; on `snapshotErr` clean stale `pre-restore-*.zip` sidecar. | **P1** |
| 12 | **Staging lifecycle — which layer is durable?** | `kopia/repo/content/content_manager.go:107 pendingPackInfo.currentPackData *gather.WriteBuffer` → `516 PutBlob` + session marker commit; `restic/repository/packer_manager.go` in-mem buffer → `Flush` uploads pack | `local.go:275 OpenFile(partial,O_CREATE|O_EXCL)→ temp.Sync→ 391 Rename(partial→backupPath)` + `787 writeMetadata` atomic `CreateTemp→Chmod0600→Rename`+`syncDirectory:816` — correct; `s3.go:126 stages via local.Create then 278 os.Open→manager.Uploader Upload` then `defer local.Delete:135` only on success; `service.go:271 UploadBackupWithOptions` buffers `io.ReadAll:303` then single `adapter.Upload` — **no temp file, no streaming, no `.partial`**; `restore.go:809 createTempRestoreDir` uses `os.TempDir()/gamepanel-restore-<uuid>:1169` + `defer RemoveAll`; `storage_s3.go:831 LocalStorageAdapter.UploadStream` sparse bug: `Truncate(size) then io.Copy appends past region → size+len(reader)` mismatched | **DIVERGED — SERVICE REGRESSES ON DURABILITY** | Beacon local path is correctly atomic; service upload path is least durable (RAM-only) vs beacon file-backed. `List:415` per-file `readOrCreateMetadata` may `776 calculateChecksum` if sidecar missing → sequential 100×SHA256 on cold start; S3 path does N+1 `HeadObject:187` per `ListObjectsV2` page. | Remove `Truncate` or `Seek(0)` after truncate; wire `service.go:271` to streaming `UploadStream` pipeline; add staging reaper for service temp dirs. | **P1** |
| 13 | **Progress WS end-to-end — re-verified dead path** | `kopia/cli/cli_progress.go:42 300ms Throttle.ShouldOutput` + `snapshotfs/snapshot_verifier.go:151` + counters `hashedFiles/cachedFiles/uploadedBytes` + `timetrack.Estimator` for `%` & `Remaining` · `restic/internal/ui/backup/json.go:42 statusUpdate{percent_done,total_bytes,bytes_done,seconds_remaining,current_files}` every 0.2s + `progress.go:59 Updater` + `rateEstimator:73` | `beacon/backup.go:34 BackupProgress{BytesProcessed,TotalBytes,Phase}` + `local.go:111 reportProgress` (publishes to `ProgressFunc` — only 3 call sites: `241 creating 0,0` + `273 archiving 0,0` + `411 completed size,size` — **TotalBytes=0 until completion**), `Restore:471 defer reportProgress completed` — no per-file increment, no hashed vs uploaded split; `server.go:62 BackupProgressEvent` + `1542 SetProgressCallback→eventBus.Publish(BackupProgressEvent+":"+serverID)` + `352 GET /servers/{id}/ws/backup` + `2155 backupProgressWS` (scope `ScopeWebsocket|ScopeBackupDownload:2169`, `Subscribe per server:2192`, `pingWebSocket:25s`) **BUT** `forge/api/http/realtime.go:124 realtimeProxy` + `forge/api/http/server.go:1995` only proxies `stats|logs|console` — **no `ws/backup`**; `server.go:2408 IssueWSTicket` only for those three; `forge/web/lib/api/ws/websocket-manager.ts:1` only for console/stats; `forge/web/components/server/backups-view.tsx:43` polls `refetchInterval 3000` when `pending||running` otherwise no polling, shows `TotalBytes=0` until completion, no bar/phase/ETA; `SetProgressCallback:78` overwrites single func pointer per adapter — concurrent backups for two servers race | **UNWIRED — DEAD END (STILL BROKEN CORE)** | Beacon teaches "backup progress exists"; panel drops it. Integration 90% done but delivers no value — incident responders debug proxy instead of missing `TotalBytes` estimation. Admin `JobService:job.go:281 Update` + `restore.go:836 persistRestore` persists `CurrentPhase` but not consumed by daily tab. Final-parity BKP-08-08, phase-03 F-B-15, subagent-05 L02 **re-verified**: grep `backup.*ws|backupProgress` in `forge/web` → 0 hits (see subagent-05 §12); `forge/api/http/server.go:1995` hard-codes `stats/logs/console` union; `lib/api.ts:933` helper omits `backup`; no `forge/web --include=*.ts backup.*ws` matches. Even bridged today WS would forward `0,0,archiving files` forever — no rate/ETA without throttled per-file progress. Kopia/Restic both throttle to 1/s (`timetrack.Throttle`, `progress.CalculateProgressInterval`). | Add `backup` to `realtimeProxy` union + `IssueWSTicket` + `lib/api.ts` union, consume WS from `BackupsView` via `WebSocketManager`; set `TotalBytes` via pre-scan `Stat` of staging `.zip` or `fs.Walk` estimate early; throttle `reportProgress` 1/s (`Throttle.ShouldOutput`) and expose per-file `BytesProcessed`. | P2 (operability P0) — **STILL BROKEN** |
| 14 | **StorageLocality scoring (Longhorn) — dead + vocab drift** | `longhorn/chart/templates/crds.yaml:4592 DiskSelector / 4636 NodeSelector` on Volume spec + `20200819-keep-a-local-replica-to-engine.md:dataLocality` (`disabled/best-effort/strict-local`) + `20251001-replica-balance-scheduling.md:17` filter chain; one enum defined once on Volume spec enforced by controller | `forge/api/internal/domain/domain.go:123 PlacementRequest.StorageLocality` (string) · `forge/api/internal/placement/strategy.go:42 Candidate.StorageLocality / 53 WorkloadRequest.StorageLocality` · `forge/api/internal/services/scheduler/service.go:212 FilterNodes ("local_only" + RuntimeProvider != "local")` · `317 ScoreNodes (req.StorageLocality != "" && r.StorageLocality != req.StorageLocality → -1e10, == → +1e8)` · `714 nodeToCandidate (storageLocality:="local"; nfs\|shared → "shared"; otherwise "local")` · `envaffinity/explain.go:104` builds candidates without StorageLocality | **DEAD + VOCAB DRIFT — RE-VERIFIED** | No production caller populates `domain.PlacementRequest.StorageLocality` — `forge/api/internal/http/handlers_servers.go:860` builds request field-by-field and **omits** it; only debug `/placement/explain` JSON bindings carry it (`http/phase6_registrar.go:73`). Therefore `±1e10/1e8` scoring and hard filter are dead code (phase-05 `F1` exact). If later wired, it **still cannot match**: candidates are `"local"/"shared"` while evacuator enum is `local_only/replicated/shared` (`evacuationplanner/service.go:28`); request `local_only != local` → every node takes permanent `-1e10` penalty. `explain` path understates locality effects (candidates unlabeled). Re-verified `grep StorageLocality` shows zero production population except struct field + scoring. | Placement locality/mount-aware at schedule time per Longhorn discipline: Forge checks mounts only *after* placement via `store_mounts_ext.go:297 ensureMountAvailableForServer` → "mount unavailable for this server node" post-hoc operator error (`phase-05 F3`). | Unify enum once (keep evacuator vocabulary `local_only|replicated|shared`), fix `nodeToCandidate` spelling, populate `StorageLocality` from egg mount analysis or template metadata, join `mount_node` availability into candidate construction; else delete dead scoring. | P2 (control-plane) — **FINDING BKP-RV16-06** |
| 15 | **Beacon verify vs Forge verify (integrity gates before prune)** | `kopia/command_snapshot_verify.go:18 snapshot verify --verify-files-percent` + `command_content_verify.go` · `restic/cmd_check.go:check` (`--read-data` re-reads pack payloads, verifies MAC) + `prune.go:119` integrity guard before delete | `beacon/verification.go:31 VerifyBackup (Download + io.Copy(hasher,reader) + EqualFold)` + `80 VerifyAllBackups` sequential per-backup, not scheduled, no percent sampling, no parallelism; `forge/api/internal/services/backup/artifact.go:515 ArtifactService.Verify (if IsVerified return nil — never re-verifies) + verifyChecksum:581` wrapper; `service.go:513 VerifyStorageReceipt`/`451 VerifyChecksumFromStore` not invoked automatically; `restore.go:1154 verifyDatabaseRestore/verifyAppRestore` are **stubs** (log + nil) in `daemonClient` path; `beacon/server.go:1706 backupMu` + S3 blocking verify `handlers_servers.go:2191` with `5m` timeout re-downloads full 20 GiB → `context deadline exceeded` indistinguishable from corruption | **PARTIAL — UNSCHEDULED + STUBBED** | Stale `IsVerified=true` trusted forever; bit-rot after initial success is permanent (Kopia always recomputes). No streaming verify with progress despite `VerifyBackup` allocating `sha256.New` that could report per-32 KiB `BackupProgress`. `git diff` shows no `VerifyAllBackups` scheduler wired. | Expand to weekly `BackupVerifyScheduler` persisting `LastVerifiedAt` + `VerificationAttempts`, forcing full re-hash for `locked` backups, with range reads & parallelism (8 workers like `restorer:verifyFile:618`). | P2 |

> Additional reconciled concerns (phase-03 F-B-11 path-suffix, F-B-12 sync execution inside HTTP/tick not reaped, F-B-16 full-download verify timeout, F-B-17 pending zombie after page unload, F-B-18 storage/retention saves are no-ops) remain DEAD per `subagent-04 L02` upload `HasSuffix` vs `Contains` `service.go:277 vs 364` `backup.test → .tar.gz` vs bare, and `subagent-05 L01` `handlers_servers.go:2044-2076` 15-min `pending→failed` goroutine with no sweeper enumerating `pending` → `failed` after `18m`. They are tracked as P2/P3 and omitted from the 14-row parity budget but remain **STILL BROKEN** in `findings` below.

---

## 3. High-Impact Logic Findings (6 — all STILL BROKEN, ≥3 required)

### BKP-RV16-01 — Unauthenticated sidecar + nil AAD → cross-server transplant forgery | P0 — STILL BROKEN
- **Re-anchors:** Phase-03 `F-B-01` + `F-B-04` + final-parity `BKP-08-01` + `BKP-08-03`
- **Evidence (HEAD file:line):** `beacon/local.go:787 writeMetadata` writes `localMetadata{Checksum,SIZE,Created}` as plain JSON `.metadata.json` (`792 CreateTemp 0600 + 802 Write + 813 Rename + 816 syncDirectory`) — body unkeyed; `757 readOrCreateMetadata` returns `metadata.Checksum` verbatim when sidecar exists (`759-767` no re-hash) → tamper bypass; `verification.go:64 EqualFold(actual, info.Checksum)` trusts that sidecar; `s3.go:27 checksumMetadataKey="sha256"` stored as S3 user-metadata (unkeyed, S3 allows overwrite); `services/backup/encryption.go:92 deriveEncryptionKey(master,nil,purposeEncryptionKey)` **nil salt** + single global purpose `gamepanel-backup-encryption:18`, `119 gcm.Seal(nil,nonce,data,nil)` **nil AAD** + `26 GetEncryptionKeyFromEnv` bypasses `secrets/keyring.go:75 envelope forge:v1:<keyID>:<b64(nonce||seal)>` (keyring correctly uses AAD); `beacon/encryption.go` missing; `backup.go:59 BackupInterface` trusts caller `namespace/name` (`validNamespace:112`/`validBackupName:125` charset/length only) — no server binding.
- **Reference contrast:** Kopia per-blob HKDF + per-write random nonce + content hash in index = transplant fails MAC; Restic `repository.go:425 verifyCiphertext` decrypt+`Hash(plaintext)==id` + 16B nonce per blob — transplant fails `Hash` gate; Longhorn has no cross-volume transplant because `DiskSelector/NodeSelector:4592` binds locality.
- **Effect at HEAD:** Attacker with write to local FS (beacon host is multi-tenant adjacent) or shared S3 bucket (panel-global credentials `048_s3_backup_config.sql:5`) can overwrite `backup.zip` + sidecar/metadata consistently → `VerifyBackup:66 EqualFold` still passes, `Restore:512 EqualFold` passes on local path, S3 path `s3.go:318-368` validates `expected` from S3 metadata *before* writing `localMetadata` but that metadata is itself attacker-writable. Even without sidecar tamper, GCM nil AAD means ciphertext for `server-A/backup-20260101T000000Z.zip` validates identically under `server-B` if name spoofed → silent cross-tenant restore of wrong-server data. No evidence of fix in `git diff HEAD -- encryption.go beacon/local.go` (zero diff). `beacon/server.go` install scope hardening does not touch backup crypto.
- **Fix (ADOPT):** Per-backup subkey `HKDF(activeKey, serverID:uuid, salt=random16)`; envelope `forge:v1:<keyID>:nonce||Seal(aad=serverID:backupName:uuid)`; store `nonce||ciphertext` with `keyID` header via `keyring`; on `Restore:504` ignore stored checksum and recompute expected from ZIP **and** AEAD tag, ignore sidecar if mismatch (fail closed, remediation log); HMAC sidecar with node secret as interim.

### BKP-RV16-02 — EncryptReader buffers entire backup → heap OOM | P1 (availability/DoS) — STILL BROKEN
- **Re-anchors:** Phase-03 `F-B-02` + `F-B-07` + final-parity `BKP-08-02`
- **Evidence (HEAD file:line):** `encryption.go:150 EncryptReader` (`158 input, _ := io.ReadAll(r) → 162 encryptWithKey(input, derived) → 166 io.Pipe replay`) + `178 DecryptReader` same double-`ReadAll`; `service.go:303 dataBytes, _ := io.ReadAll(data)` then `316 adapter.Upload(ctx,backupPath,dataBytes)` + `service.go:368 Download` into `data []byte` then `379 Decrypt(data,key)` in-place; `artifact.go:591 Download (bytesReader)` and `877 downloadToTempFile` (whole-file buffering) bypass streaming `StorageAdapter.UploadStream/DownloadStream:storage.go:35` which exist but are unused on encrypted path; `s3.go:278 os.Open(localPath)→manager.Uploader Upload` streams correctly but service hot path clobbers it. `encryption_test.go:146 uses 100 KiB fixture — never catches. `git diff` shows zero streaming fix.
- **Reference contrast:** `kopia/repo/content/content_manager.go:334 shouldWrite >= MaxPackSize` + streaming `io.Pipe` per chunk + `repo/blob/throttling/throttler.go:26` token-bucket; `restic/repository.go:380 saveAndEncrypt` streams 16 MiB packs via `packerManager` — never buffers whole repo.
- **Effect at HEAD:** World 20 GiB → API RAM spikes ≥20 GiB + zstd `compression.go:63 CompressReader` `io.Pipe gzip/zstd` + hex nonce copy `service.go:312 nonceHex` leaks → OOM-killing `api` pods (default 1 GiB limit in `infra/compose.yml`). Vulnerability window: manual uploads and DB-volume backups via service layer (`service.go:271`) and any `FORGE_MASTER_KEY` rotation re-encrypt path. `LocalStorageBackend.UploadStream:storage_s3.go:831` sparse bug (`Truncate(size)` then `io.Copy` appends → `size+len(reader)`) compounds resumability failure.
- **Fix (ADOPT):** Chunked AEAD streaming `EncryptedWriter(frame=64KiB, nonce(seq)+GCM, header seq)` + `EncryptedReader` wired through `UploadStream/DownloadStream` `io.Pipe` without intermediate `ReadAll`; cap `maxUploadBytes` + `maxS3DownloadBytes:27` enforcement on upload path; `storage_s3.go:831` remove `Truncate` or `Seek(0)` after truncate.

### BKP-RV16-03 — Retention AND vs OR inversion (three→four engines diverge) — data-loss | P1 — STILL BROKEN
- **Re-anchors:** Phase-03 `F-B-05` + `F-B-06` + `F-B-13` + subagent-04 `L01` De Morgan correction + final-parity `BKP-08-04` + `BKP-08-09`
- **Evidence (HEAD file:line):** `beacon/retention.go:61` "Intersection: ALL rules" (AND) initialized `keep=all` then `104 MaxBackups` intersect again + `79 KeepDaily/KeepWeekly/KeepMonthly` re-sets `keep[b.ID]=true` even for backups rejected by `MaxAge:64` (bypasses age gate) then `104 MaxBackups` intersects → AND/OR hybrid; `service.go:768 keep = isLocked || (withinCount && withinAge)` pure AND; `artifact.go:711 ApplyRetentionPolicy:738` (`(!exceedsCount && !exceedsAge)`) De Morgan-equivalent AND but beacon diverges; `store_backup_policies.go:301 ListExpiredBackups hard-coded 30 days` ignoring per-policy `RetentionDays`; `worker.go:401 enforceRetentionBeforeBackup:422 overLimit = count - MaxBackups +1` count-only pre-delete then calls `768` AND — double enforcement deletes more than either alone; `104_a_backup_system.sql:29 backup_configurations/jobs/artifacts/restores` never unified with `backup_policies`; both tables written/concurrently; `178_backup_retention.sql` adds fourth engine.
- **Reference contrast:** Kopia retention is **union OR** (keep if *any* bucket keeps, locked exempt); Restic `ForgetPolicy` `ApplyPolicy` union → oldest of each time-bucket kept, locked exempt, `prune` rebuilds index under exclusive `lock.go:47`. Forge explicitly opposite (`retention.go:61` comment says intersection opposite to both references).
- **Effect at HEAD:** Policy `MaxBackups=10, RetentionDays=7, KeepDaily=5` with 20 backups aged 10d: Restic/Kopia keep ~10 (union); Forge AND keeps 0 unlocked (`withinAge=false` → delete) save `122 safetyRail keep[backups[0].ID]=true` single newest — 9 days of history unexpectedly discarded. Conversely beacon can retain `>MaxAge` via `KeepDaily:79` → bypass. `Worker.enforceRetentionBeforeBackup:409 ListBackups 1,1000` then `433 sort.SliceStable oldest-first` without `FOR UPDATE SKIP LOCKED` → concurrent API replicas race `436 DeleteBackupFromStorage` + `441 DeleteBackup` without Tx → thundering herd, metric double-count, non-atomic; `store_backups.go:242 CleanupOldBackups` DB-only orphans S3 (hard to reconcile). `git diff` shows zero retention fix.
- **Fix (ADOPT):** Single `RetentionEngine` implementing **union OR** (`keep if any bucket keeps || isLocked`), property-tested against Restic vectors; remove `CleanupOldBackups` SQL-only path; parameterize `ListExpiredBackups` by policy; add `backup_storage_receipts` GC + `SELECT … FOR UPDATE SKIP LOCKED`.

### BKP-RV16-04 — SQL-only prune orphans S3 / racy retain-before-backup — no exclusive lock | P1 — STILL BROKEN
- **Re-anchors:** Phase-03 `F-B-06` + final-parity `BKP-08-05`
- **Evidence:** `store_backups.go:242 CleanupOldBackups (DELETE WHERE is_locked=FALSE AND created_at < now()-interval '1 day'*$1 AND status='completed')` — DB-only `248-253`; `277 CleanupOldBackupsForServer 277-295` combines age OR subselect `IN (SELECT uuid ... ORDER BY created_at ASC LIMIT GREATEST(0,COUNT- $3))` also DB-only, not deterministic under concurrent inserts and can delete just-completed newest backup before `FailStaleBackups:348` finalizes it (subagent-01 LF-02); `service.go:689 CleanupExpiredBackups` correctly iterates `ListExpiredBackups` and calls `DeleteBackupFromStorage` then `DeleteBackup` but not sole caller; `worker.go:406 enforceRetentionBeforeBackup` iterates oldest-first without `FOR UPDATE SKIP LOCKED`, metric `cleaned++` double-counts on race; `s3.go:163 List` N+1 `HeadObject:187` per object to re-derive `Checksum` adds cost; `store/store.go:45 pg_advisory_lock` only for migrations — no lock in backup prune path.
- **Reference:** `restic/repository/prune.go:119 ErrSizeNotMatching` + `decidePackAction:386` reachability check before delete — Forge has no `read-data` integrity gate before prune; `lock.go:47 exclusive` vs Forge no gating.
- **Effect:** Long-run orphan growth (billing), S3 `List` vs DB count drift (`s3.go:163` still serves orphans); worst-case deletes newest live backup if `DELETE … LIMIT … ORDER BY created_at ASC` races concurrent `FailStaleBackups`/`UpsertBackup`.
- **Fix:** Single ownership `DELETE … RETURNING` + storage delete in Tx or mark sweep (`status='pruned_pending'` → GC worker), nightly `ListObjectsV2 vs SELECT uuid` reconciliation like `restic check --read-data`; gate every prune with `pg_advisory_xact_lock(hashtext('backup_prune:'||serverID))` or S3 conditional `locks/backup-<server>.json` TTL+refresh mirroring `lock.go:47`.

### BKP-RV16-05 — No cross-host distributed lock; in-memory lockNamespace split-brain | P1 — STILL BROKEN
- **Re-anchors:** Phase-03 `F-B-08` + final-parity `BKP-08-06` (expands subagent-02 #12)
- **Evidence:** `local.go:48 namespaceMu + namespaceOps[ns].mu` per-namespace in-process `sync.Mutex` guards only `Create:239` + `Restore:469`, **not** `List/Get/Delete/Download:415-466`; cross-host: no persistent lock file, no advisory row; `worker.go:201 executePolicy` builds `name=backup-%s:211 Format("20060102T150405Z")` second-truncated, two workers same second → same `(server_id,name)` → second `ON CONFLICT` overwrites first UUID (`store_backups:54`); daemon side second `Create` then `OpenFile(partial,O_CREATE|O_EXCL,0600):275` fails `exists` for same daemon but if different daemons each has isolated `backupRoot`/S3 prefix both succeed — two S3 objects same logical name different `Checksum` race to `UpsertBackup:256-268` second overwrites; S3 staged upload no lock; restore journal path `896 ".<base>.restore-journal.json"` local-FS only → dual restore on shared `serverRoot` (NFS) creates independent journals, torn phases leave `parent/.rollback-*` orphans.
- **Reference:** `restic/lock.go:47-111 LockRepo(exclusive, retryLock, printRetry)` writes backend lock file and refreshes `124` with stale detection `188`; `prune` demands exclusive and fails if any non-exclusive lock exists; Kopia `blob.Storage` atomic `PutBlob(DoNotRecreate)` + `content/sessions.go` + `content_manager.go:70 cond`.
- **Effect at HEAD:** Scaled beacon or host failover → duplicate backups, ambiguous "latest" (`store_backups.go:18 ORDER BY created_at DESC` hides duplicate-name collision), torn restore interleaves journal phases leaving `.rollback-*` dirs orphaned (reaper `RecoverRestoreJournals:867` only scans `serverDataRoot` for `.*.restore-journal.json` at boot before reconstruction — not S3 `.s3-download-*`). No fix in current `beacon/server.go` diff (only install scope hardening).
- **Fix:** Backend-anchored lock — `pg_advisory_xact_lock(hashtext(serverID))` around `executePolicy` or S3 `locks/backup-<serverID>.json` TTL+refresh (mirror `lock.go:47`); gate `Worker.tick:119` with `queue/leader.go` elector; persist multipart state for S3 resume if needed.

### BKP-RV16-06 — StorageLocality scored but can never fire + three vocabularies disagree | P2 — STILL BROKEN (dead code)
- **Re-anchors:** Phase-05 subagent-04 `F1` + `F3` + final-parity `BKP-08-09`
- **Evidence:** `forge/api/internal/domain/domain.go:123 PlacementRequest.StorageLocality` (string) · `forge/api/internal/placement/strategy.go:42 Candidate.StorageLocality / 53 WorkloadRequest.StorageLocality` · `forge/api/internal/services/scheduler/service.go:212 FilterNodes (StorageLocality=="local_only" && RuntimeProvider != "local")` + `317 ScoreNodes (±1e10/1e8)` + `714 nodeToCandidate storageLocality:="local"; nfs\|shared → "shared"; otherwise "local"` · `envaffinity/explain.go:104` builds candidates without StorageLocality. Handler `forge/api/internal/http/handlers_servers.go:860` builds `PlaceServer` request field-by-field and **omits** `StorageLocality` — only debug `/placement/explain` JSON bindings carry it. Therefore scoring and hard filter are dead (`phase-05 F1`). If later wired, it **still cannot match**: candidates are `"local"/"shared"` while evacuator enum is `local_only/replicated/shared` (`evacuationplanner/service.go:28`); request `local_only != local` → every node takes permanent `-1e10` penalty (`scheduler/service.go:714`). Three spellings across three packages (`local`, `local_only`, `replicated`).
- **Reference:** Longhorn single enum (`disabled/best-effort/strict-local` `20200819-keep-a-local-replica-to-engine.md:29`, enforced by same controller that schedules `20221123-local-volume.md:48`) — one vocabulary, schedule-time rejection via `diskSelector:4592/nodeSelector:4636`.
- **Effect:** Dead code implying enforcement while eligibility is checked post-placement via `store_mounts_ext.go:297 ensureMountAvailableForServer` ("mount not available for this server node and egg") — Longhorn prevents this at schedule time; Forge turns it into post-hoc operator error. Wiring later without fixing vocab → every node penalized `-1e10`, `explain` path understates locality effects.
- **Fix:** Unify enum once (keep evacuator vocabulary), fix `nodeToCandidate:714` spelling, populate `StorageLocality` from egg mount analysis or template metadata, join `mount_node` availability into candidate construction; else delete dead scoring to stop implying enforcement.

> **Aggregate still-BROKEN verdict:** All 7 task-mandated items remain **BROKEN/DEAD** at HEAD: (1) sidecar transplant P0, (2) OOM `io.ReadAll`, (3) retention AND vs OR, (4) prune without lock (SQL-only), (5) GC orphan `.partial/.restore-*/.s3-download-*`, (6) lockNamespace in-process only, (7) dead progress WS (`server.go:1995` + `realtime.go:124` + `local.go:111 TotalBytes=0`). Six additional high-risk divergences (chunked dedup missing, pre-snapshot vacuous, staging durability regress, scheduling first-run dead, verify unscheduled/stubbed, locality dead) also **STILL BROKEN** — total ≥13 broken/dead rows out of 15 parity rows.

---

## 4. Reconciliation — Why 18 + 9 Were Not Double-Counted

Phase-03 synthesis captured **18** backup findings across 5 subagents (4 crypto, 3 retention/GC, 4 concurrency/journal, 3 UX dead-progress, 4 duplicates). Final-parity subagent-08 captured **20 parity rows** and **9 logic findings** `BKP-08-01..09` that **re-anchored and expanded** the same 18 (e.g., Phase-03 `F-B-01/F-B-04` LF-02 → `BKP-08-01`/`BKP-08-03` with nil AAD + plaintext beacon; `F-B-05` → `BKP-08-04` with four-table divergence). Reverification eliminated double-counting by mapping:

| Phase-03 | Final-parity | Reverification | Still BROKEN? | Evidence delta |
|----------|--------------|----------------|---------------|----------------|
| F-B-01, F-B-04, L1, LF-02 | BKP-08-01, BKP-08-03 | **BKP-RV16-01** | YES — nil salt/AAD + plaintext beacon + unkeyed sidecar all still present | Added `s3.go:27` S3 metadata unkeyed + daemon bypass `restore.go:1154` stub |
| F-B-02, L2, LF-01 | BKP-08-02 | **BKP-RV16-02** | YES — `encryption.go:150` + `service.go:303` double `ReadAll` unchanged | Added `storage_s3.go:831 Truncate` sparse bug + S3 streaming contrast `s3.go:278` |
| F-B-05, F-B-13, L3, L01 | BKP-08-04 | **BKP-RV16-03** | YES — AND still `retention.go:61` explicit | Added `worker.go:401 count-only` double-enforcement |
| F-B-06, F-B-07, L4, LF-02 | BKP-08-05 | **BKP-RV16-04** + row 08 orphan | YES — `store_backups.go:240 DB-only` still sole caller via cron | Added N+1 `HeadObject` cost `s3.go:187` |
| F-B-08, L3, LF-03 | BKP-08-06 | **BKP-RV16-05** | YES — `local.go:90` map still in-process only | Added `worker.go:211` second-truncated name collision |
| F-B-15 | BKP-08-08 + L02 | Row 13 **DEAD** | YES — `server.go:1995` still `stats/logs/console` only | Added `SetProgressCallback:78` single-slot race + `local.go:111 TotalBytes=0` forever |
| F-B-03 (unthrottled fan-out) | BKP-08-08 note | Row 13 throttling gap | YES — `local.go:292 rate.Limiter` only on ZIP create write path | Kopia `throttle.ShouldOutput` guard missing |
| Phase-05 F1 (locality dead) | BKP-08-09 | **BKP-RV16-06** row 14 | YES — `handlers_servers.go:860` still omits field | Added `envaffinity/explain.go:104` unstamped candidates |

Net unique still-BROKEN high-impact findings: **6** (`BKP-RV16-01..06`) — no finding was fixed or partially fixed except `store_backups.go:64 isLocked` param alignment (unrelated to the 6 cores). No prior finding was a false positive — re-inspection confirmed each file:line still triggers.

---

## 5. What *Did* Change (and What Didn't)

**Non-core fixes observed in `git diff HEAD` that do NOT resolve the 7 BROKEN cores:**

- `forge/api/internal/store/store_backups.go:64` — `UpsertBackup` param list shifted to include `isLocked` (was silently dropping `FALSE` → misalignment) and new `CountAllBackups:389` helper. Does not touch retention semantics, sidecar, crypto, or GC.
- `beacon/internal/server/server.go:1539-1553` — `sanitizeBackupName` + idempotency `Get` before `Create` for retry-after-response-loss (adds `name` JSON field). Does not fix unauth sidecar trust, OOM, retention, or progress WS dead. `backupMu:77` still global per-beacon, not distributed; `SetProgressCallback:1542` still overwrites single slot.
- `beacon/internal/server/server.go:223 manager.SetStateDir` + install `BeginInstall/EndInstall` hardening — unrelated to backup pipeline.
- `forge/api/internal/http/server.go:1995` / `realtime.go:124` — no `backup` stream added; `ws/ticket` logic unchanged; `hostTerminalWS` added — unrelated.

**Zero diff (still BROKEN):**
- `forge/api/internal/services/backup/encryption.go` — still `nil` salt/AAD, `io.ReadAll` both paths
- `beacon/internal/backup/retention.go:61` — still `Intersection` AND + `122 safetyRail`
- `beacon/internal/backup/local.go:90` / `757` / `787` / `275` / `532` — still in-process lock, trusted sidecar, `.partial` no GC, 3-phase journal only for truncate
- `forge/api/internal/services/backup/service.go:271` / `743` / `689` — still `ReadAll` + AND `withinCount && withinAge` + `CleanupExpiredBackups` loop without lock
- `forge/api/internal/services/backup/worker.go:152` / `401` — still `NextRunAt==nil => false` + count-only pre-delete
- `forge/api/internal/store/store_backups.go:240` / `263` — still DB-only `DELETE`
- `beacon/internal/backup/verification.go:31` — still full-download `VerifyBackup`
- `forge/api/internal/http/realtime.go:124` + `server.go:1995` — still no `backup` proxy

**Reference stability:** `kopia/repo/blob/storage.go:204`, `kopia/repo/content/content_manager.go:70`, `restic/chunker.go:9`, `restic/prune.go:106`, `restic/lock.go:47` — all re-read HEAD and match prior citations (no upstream drift relevant to parity).

---

## 6. Verdict

**All 7 task-mandated BROKEN items remain BROKEN/DEAD at HEAD — no incident was closed by product changes since Phase-03 / Final-Parity:**

| Mandated item | Reference line | Forge file:line re-verified | Status |
|---------------|----------------|-----------------------------|--------|
| Unauth sidecar transplant P0 | `kopia/repo/content/content_manager.go:70` per-blob MAC vs `restic/repository.go:425 verifyCiphertext` | `encryption.go:92 nil salt` + `119 nil AAD` + `beacon/local.go:757 trust` + `787 plain JSON` + `s3.go:27 unkeyed metadata` | **BROKEN P0** |
| Streaming buffer OOM | `kopia/repo/blob/throttling/throttler.go:26` + `restic Repository:380` 16 MiB packs | `encryption.go:150 io.ReadAll` + `service.go:303 io.ReadAll` + `311 nonceHex` | **BROKEN P1** |
| Retention AND vs OR | `kopia/snapshot/policy/retention_policy.go:61 union` + `restic/policy.go:ApplyPolicy` union | `beacon/retention.go:61 Intersection` + `service.go:768 AND` + `store_backup_policies.go:301 30d` | **BROKEN P1** |
| Prune without lock | `restic/lock.go:47 exclusive` + `prune.go:119 / 594` | `store_backups.go:240/263 DB-only DELETE` + no `pg_advisory_lock` | **BROKEN P1** |
| GC orphan .partial / .s3-download-* | `kopia maintenance GC roots` + `restic prune.go:386 reachability` | `local.go:275 .partial O_EXCL` + `s3.go:296 .s3-download-*` + no reaper (only `backup_jobs` SQLite sweep) | **BROKEN P1** |
| lockNamespace in-process only | `restic/lock.go:47 backend lock file` + `kopia blob PutBlob(DoNotRecreate)` | `beacon/local.go:90 namespaceMu map` + guards only `239/469` | **BROKEN P1** |
| Dead progress WS | `kopia/cli_progress.go:42 300ms Throttle.ShouldOutput` + `restic/ui/backup/json.go:42 statusUpdate every 0.2s` | `beacon/server.go:2155 backupProgressWS` exists but `forge/api/http/server.go:1995 realtimeProxy` hard-codes `stats|logs|console` — no `backup`; `local.go:111 TotalBytes=0` until done | **DEAD P2 (operability P0)** |

**StorageLocality (Longhorn) is confirmed DEAD per Phase-05 F1 — `domain.go:123` field + `scheduler/service.go:212/317/714` scoring are dead code (`handlers_servers.go:860` omits it) and vocab-drifted (`local` vs `local_only` → `-1e10` on any future wire).** Longhorn discipline (`crds.yaml:4592/4636` schedule-time `DiskSelector/NodeSelector` + `dataLocality` enum) remains unadopted; Forge still checks mounts post-placement `store_mounts_ext.go:297`.

**Risk roll-up (reverified):**
- **P0 data-integrity/trust:** BKP-RV16-01 (transplant forgery) + BKP-RV16-02 (OOM availability for >300 MiB encrypted) block any multi-tenant/S3-shared claim.
- **P1 data-loss/cost:** BKP-RV16-03 (retention inversion silently deletes 9/10 backups) + BKP-RV16-04 (orphan S3 growth + racy deletes newest) + BKP-RV16-05 (split-brain duplicate) + row 08 (orphan leak invisible) require exclusive-lock + streaming + retention unification before SLAs.
- **P2 operability:** Row 13 dead WS + Row 10 first-run-dead scheduler + Row 15 unscheduled verify mean operators perceive "backup appears dead" even though plumbing is 90% done (matches final-parity synthesis §3 paraphrased).

**Next activation order (reverified, no product code modified):** Same as final-parity §5 but with reverification anchors:
1. P0 wire `backupProgressWS` end-to-end (`server.go:1995` + `realtime.go:124` + `lib/api.ts` + set `TotalBytes` early via `Stat` — currently `local.go:111 0,0`).
2. P0 fix encryption streaming + per-backup salt/AAD (`encryption.go:150` streaming, `s3.go:278` encrypt before `PutObject`).
3. P0 unify retention to union OR (single `RetentionEngine`, deprecate `store_backups.go:240` DB-only path).
4. P1 make prune exclusive (`pg_advisory_xact_lock` around `service.go:689` + mark sweep + nightly reconciliation).
5. P1 fix restore staging journal (clean stale `pre-restore-*.zip` on `snapshotErr:1725`, persist `RestoreOptions` in journal, rehash sidecar on tamper).
6. P1 add distributed lock (`local.go:90` → backend-anchored per Restic `lock.go:47`).
7. P2 wire StorageLocality end-to-end or delete dead scoring (`scheduler/service.go:212/317/714` + `handlers_servers.go:860` + unify vocab).

---

*Reverification subagent 16 — file:line checked at 2026-08-24 HEAD; no product code modified. All parity rows and findings cross-anchored to Phase-03 synthesis (18 findings) and final-parity subagent-08 (20 rows, 9 findings) and Phase-05 subagent-04 (StorageLocality dead).*

apiVersion: v1
kind: Secret
metadata:
  name: gamepanel-secrets
  namespace: gamepanel
type: Opaque
stringData:
  # !!! SECURITY WARNING — DO NOT APPLY THIS FILE AS-IS !!!
  # Replace ALL values below with your own generated secrets.
  # Use: openssl rand -hex 32
  # The API will REJECT startup if any value still contains CHANGE_ME (see forge/api/cmd/api/main.go production guard).
  # For production, prefer: kubectl create secret generic gamepanel-secrets --from-env-file=.env
  # or use sealed-secrets / external-secrets operator (see docs/operations/secrets.md).
  POSTGRES_PASSWORD: "CHANGE_ME_POSTGRES_PASSWORD"
  DATABASE_URL: "postgres://gamepanel:CHANGE_ME_POSTGRES_PASSWORD@postgres:5432/gamepanel?sslmode=disable"
  API_AUTH_SECRET: "CHANGE_ME_API_AUTH_SECRET"
  APP_KEY: "CHANGE_ME_APP_KEY"
  FORGE_MASTER_KEY: "CHANGE_ME_MASTER_KEY"
  DAEMON_NODE_TOKEN: "CHANGE_ME_NODE_TOKEN_ID.CHANGE_ME_NODE_TOKEN_SECRET"
  # Passphrase for the encrypted SFTP host key (>= 16 bytes; used by the
  # beacon DaemonSet to decrypt its host key).
  DAEMON_SFTP_HOST_KEY_PASSPHRASE: "CHANGE_ME_SFTP_HOST_KEY_PASSPHRASE"
  # Base64 Ed25519 public key used by the beacon to verify signed
  # remote/self-upgrade metadata.
  DAEMON_UPGRADE_PUBLIC_KEY: "CHANGE_ME_UPGRADE_PUBLIC_KEY"
  METRICS_TOKEN: "CHANGE_ME_RANDOM_32_BYTE_METRICS_TOKEN"
  DAEMON_NODE_ID: "CHANGE_ME_NODE_UUID"
  GRAFANA_ADMIN_PASSWORD: "CHANGE_ME_GRAFANA_PASSWORD"

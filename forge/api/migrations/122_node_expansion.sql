ALTER TABLE nodes
    ADD COLUMN IF NOT EXISTS display_name TEXT,
    ADD COLUMN IF NOT EXISTS public_hostname TEXT,
    ADD COLUMN IF NOT EXISTS listen_port_min INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS listen_port_max INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS allowed_ips TEXT[] DEFAULT '{}',
    ADD COLUMN IF NOT EXISTS network_interface TEXT,
    ADD COLUMN IF NOT EXISTS daemon_ssl_cert TEXT,
    ADD COLUMN IF NOT EXISTS daemon_ssl_key TEXT,
    ADD COLUMN IF NOT EXISTS auto_connect BOOLEAN NOT NULL DEFAULT true,
    ADD COLUMN IF NOT EXISTS connection_retries INTEGER NOT NULL DEFAULT 3,
    ADD COLUMN IF NOT EXISTS heartbeat_interval INTEGER NOT NULL DEFAULT 15,
    ADD COLUMN IF NOT EXISTS cpu_cores INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS reserved_memory_mb INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS reserved_disk_mb INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS default_allocation_ip TEXT DEFAULT '0.0.0.0',
    ADD COLUMN IF NOT EXISTS allocation_port_min INTEGER DEFAULT 25565,
    ADD COLUMN IF NOT EXISTS allocation_port_max INTEGER DEFAULT 26565,
    ADD COLUMN IF NOT EXISTS auto_allocate BOOLEAN NOT NULL DEFAULT false,
    ADD COLUMN IF NOT EXISTS backup_directory TEXT,
    ADD COLUMN IF NOT EXISTS transfer_directory TEXT,
    ADD COLUMN IF NOT EXISTS mount_points JSONB DEFAULT '[]',
    ADD COLUMN IF NOT EXISTS token_rotation_policy TEXT DEFAULT 'manual',
    ADD COLUMN IF NOT EXISTS firewall_rules JSONB DEFAULT '[]',
    ADD COLUMN IF NOT EXISTS tls_setting TEXT DEFAULT 'auto',
    ADD COLUMN IF NOT EXISTS enable_health_checks BOOLEAN NOT NULL DEFAULT true,
    ADD COLUMN IF NOT EXISTS enable_metrics BOOLEAN NOT NULL DEFAULT true,
    ADD COLUMN IF NOT EXISTS prometheus_endpoint TEXT,
    ADD COLUMN IF NOT EXISTS alert_threshold_cpu INTEGER DEFAULT 90,
    ADD COLUMN IF NOT EXISTS alert_threshold_memory INTEGER DEFAULT 90,
    ADD COLUMN IF NOT EXISTS alert_threshold_disk INTEGER DEFAULT 90,
    ADD COLUMN IF NOT EXISTS maintenance_message TEXT,
    ADD COLUMN IF NOT EXISTS drain_before_maintenance BOOLEAN NOT NULL DEFAULT false,
    ADD COLUMN IF NOT EXISTS labels JSONB DEFAULT '[]',
    ADD COLUMN IF NOT EXISTS cluster_group_id TEXT;

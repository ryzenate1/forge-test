// Package queue is deprecated: River-style durable queue (river_job).
//
// Deprecated: Use forge/api/internal/services/queue (job_queue) as the canonical
// durable queue. This package (forge/api/queue) is retained for backward-
// compatibility only and is not wired in cmd/api/main.go. See README.md for
// audit and migration guidance.
package queue
